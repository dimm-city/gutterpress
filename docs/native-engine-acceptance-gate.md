# Native-engine acceptance gate

Status: **binding**. This is the bar the native engine must clear before it
becomes the default, and before Paged.js is dropped. Set by the product owner
2026-08-08.

> A frozen, point-in-time copy of this file's measured-results table (plus
> pointers to the rest of the durable A/B evidence, e.g. `spike/folio/`) is
> archived at
> [`docs/native-engine-parity-evidence-archive.md`](./native-engine-parity-evidence-archive.md),
> captured 2026-08-09 per `native-only-migration-plan.md`'s Phase 6
> housekeeping — so the proof survives even after Paged.js is eventually
> deleted and this living doc has moved on.

## The rule

> **Provable parity, or provable improvement, versus the Paged.js
> implementation — for everything from PDF/X build output to hot reloading in
> the desktop UI.**

"Provable" means measured and reproducible on a real book, not reasoned from
code. A claim without an observation behind it does not count. Rendering and
looking beats any metric: matching page counts have twice hidden real defects
in this migration (a dead column; a half-empty page).

**The one agreed exception:** PDF build **wall-clock time**. The native engine
may be slower than Paged.js — that is an accepted, documented limitation — so
long as the *output* is as good or better. Nothing else gets an exception.

## What parity means — and what it does not

**Parity is with the book's design intent, never bug-for-bug with Paged.js.**
(Constitution: CLAUDE.md "What Gutterpress is — and what the engine is not" —
the engine and every shim exist only to fill gaps in Chrome's Paged Media
implementation and are expected to be removed as Chrome improves; they must
stay thin and standards-based so that removal is a no-op for authors.)
Chromium's native print is the standards baseline. Where the legs differ,
classify the divergence before touching anything:

1. **Engine bug** — native deviates from what the CSS specifies → fix the
   engine.
2. **Book CSS relying on a Paged.js quirk** — the CSS was written against
   Paged.js's non-standard behaviour and native is CSS-correct → fix the
   BOOK's CSS to express the intent in standard terms; record it as a
   migration note. (Example: `page: chapter-start` on a multi-sheet container
   suppresses margin chrome on every sheet natively — spec-correct — where
   Paged.js applied it to one page; the field guide's "missing opener chrome"
   is this class, not an engine regression.)
3. **Paged.js bug** — native is right and better → record it as a deliberate
   improvement (the design-guide sidebar float, measured 38% of its
   containing block natively vs 9% off under paged, is this class).

**Never replicate a Paged.js quirk inside the engine to make a diff go
green.** A "fix" that moves the engine away from the spec to match the
polyfill is a regression by definition, whatever the diff says.

## What must be proven

Each row needs a measured result on a real book, native vs paged, before the
gate can be called clear. `=` means parity; `>` means native is better; `<` is
a blocker unless it is the wall-clock row.

### A. PDF output
1. Page count and pagination decisions (differences explained, not tolerated).
2. Type scale — no whole-document shrink on either leg (median glyph height).
3. Content fidelity: every element that prints on the paged leg prints on the
   native leg, on a comparable page. Verified by rendering pages and looking,
   plus `pdfimages -list` for art that exists in the file but is painted out
   of view.
4. Text remains extractable/searchable — no new rasterization. `pdftotext`
   coverage per page compared between legs.
5. Fonts embedded; no fallback substitution.
6. Cross-references and TOC page numbers resolve correctly (`target-counter`).
7. Running heads / folios / margin chrome present and correct.

### B. PDF/X output (`--format pdfx`)
8. PDF/X-1a and PDF/X-3 both build.
9. ICC profile embedded; output intent correct.
10. Post-build validation passes at least as well as the paged leg.
11. PDF boxes — trim/bleed/art — correct; crop marks present when requested.
12. Signature padding, metadata (title/author), annotation stripping.

### C. Preview and desktop UX
13. **Hot reload**: an edit reaches the preview at least as fast and as
    reliably as the paged leg. Measure the actual latency both ways.
14. Preview fidelity: what the author sees matches what prints — this is the
    parity gate (`scripts/native-parity-gate.ts`), which must pass with an
    **empty allowlist**.
15. Page navigation, zoom, view modes, outline.
16. Block edit / click-to-edit, context menu, editor↔preview source sync.
17. Scroll position and page position survive a reload.
18. Error and warning surfacing: build diagnostics reach the Problems panel.

### D. Author-facing behaviour
19. CSS support: anything the paged leg renders, the native leg renders — or
    the difference is a documented, deliberate improvement.
20. Build diagnostics are as good or better (native adds checks paged lacks:
    broken cross-references, abspos leaks, dead columns).
21. Error messages on failure are as actionable.

### E. Static HTML export
22. `--format html` produces a publishable, paginated artifact on the native
    leg (the paginated view is the default, always).
23. Embedding (iframe) works.

### F. Reliability
24. Determinism: same input, same output across repeated builds.
25. No new failure modes on a machine without a system Chromium (preflight
    fires early with an actionable message).

## Accepted limitation

26. **Build wall-clock time may regress.** Record the measured delta on a real
    book so it is documented rather than discovered. Everything else in this
    list must be `=` or `>`.

## How results are recorded

Every iteration appends to the results table below: date, area, native result,
paged result, verdict (`=`, `>`, `<`), and the evidence (command run, file
rendered, what was observed). A `<` stays open until fixed or explicitly
accepted by the product owner.

| Date | Area | Native | Paged | Verdict | Evidence |
|---|---|---|---|---|---|
| 08-08 | A.1 page count (field guide 300pp) | 302pp | 303pp | = | native denser (11.0 vs 10.6 lines/page) |
| 08-08 | A.1 page count (design guide) | 53pp | 54pp | = | content identical through p9 |
| 08-08 | A.2 type scale (field guide pp40/260) | 18.252pt | 18.252pt | = | `pdftotext -bbox` median glyph height |
| 08-08 | A.2 type scale (design guide pp10/25/45) | 9.966/10.959/12.176 | 9.967/10.960/12.176 | = | matches to 0.001pt |
| 08-08 | A.3 content fidelity (field guide) | 28,384 tokens | 28,180 | = | no loss; paged-only tokens are a `pdftotext` folio-chip artifact |
| 08-08 | A.6 cross-references | all resolve | all resolve | = | parity gate, 9 target-counter ids on design-guide |
| 08-08 | A.7 margin chrome (body pages) | present, mirrored | present, mirrored | = | rendered pp 11/40/150/260 both legs |
| 08-08 | **A.7 margin chrome (chapter-opener pages)** | **absent** | head + folio + chapter chips | **<** — SUPERSEDED, see the 08-08 REVIEW A.7 row below | field guide p7 rendered side by side |
| 08-08 | **A.3 float placement (field guide p7 aside)** | drops full-width below text | floats right beside text | **<** — SUPERSEDED, see the 08-08 (WP-C) and 08-08 (REVIEW) A.3 rows below | rendered p7 both legs |
| 08-08 | A.3 image objects, placement count (field guide 302pp) | 3,067 rows | 579 rows | (misleading metric — see next row) | `pdfimages -list` row count reproduces exactly on the real 302pp book |
| 08-08 | **A.3 image objects, corrected verdict (field guide 302pp)** | 347 unique XObjects, 36.8 MB unique image bytes, 167.5 MB PDF | 293 unique XObjects, 35.3 MB unique image bytes, 162.1 MB PDF | **=** | the original 3,067-row count measures `Do`-operator PLACEMENTS, not distinct bitmaps — the brick-tile XObject IS shared, not re-encoded: `pdfimages -list` shows the 1275×851 brick tile as ONE object id (43) placed 299 times (once per page); grouping every row by object id gives 347 unique bitmaps (native) vs 293 (paged), and summing each unique object's bytes gives 36.8 MB vs 35.3 MB (+4%) — the full PDF is 167.5 MB vs 162.1 MB (+3.3%). **Review pass re-measured this row on the same two artifacts: the unique-XObject counts (347 vs 293) and the PDF sizes reproduce exactly, but the two byte figures do NOT — summing each unique object's `pdfimages -list` size gives 149.9 MB (native) vs 148.0 MB (paged), i.e. +1.3%, not 36.8/35.3 MB at +4%. The brick tile is confirmed shared as ONE object (id 43, 1275×851, 226K) but with 598 placements, not the 299 recorded here. Neither correction changes the `=` verdict — it makes it slightly stronger.** Root cause of the elevated ROW count (not a defect): native paints 16 margin boxes with brick fill on every one of 302 pages (one `Do` reference per box per page — the per-page-print model CSS Paged Media requires), where Paged.js paints the SAME visual result with far fewer references because its polyfill composites the wall as a small number of large DOM elements across the whole continuous canvas rather than one CSS background per physical page. Tried to reduce: none needed — the XObject-sharing Chromium already does (confirmed above) is the mechanism that matters for file size, and it is already working. Verdict: accepted as **inherent to per-page native printing**, not a shared-resource bug; the placement count is cosmetic and does not measurably affect file size (+3.3%) or embedded image bytes (+4%). The original `<` verdict is superseded — it was measuring the wrong quantity. |
| 08-08 | **A.7 margin chrome — root cause** | isolated: `@page citizen-file:right`'s margin-box content fails to paint specifically on the FIRST page of a new named-page run; the same rule on `:left`, the same rule on later `:right` continuation pages (p9), and the generic (unnamed) `:right` pseudo on the SAME page all render correctly | n/a — polyfill unaffected | **<, root-caused** — SUPERSEDED (fixed by `fb60972`), see the 08-08 REVIEW A.7 row below | 40pt red/yellow marker replacing the rule's content still failed to paint on p7 (`@page citizen-file:right{@top-right{content:"MARKER-XYZ"...}}`); the identical technique on `:left` (p8) rendered `LEFT-MARKER-XYZ` correctly; a generic unnamed `@page :right{@top-center{...}}` marker rendered correctly on the SAME p7. Ruled out as causes (each independently tested and disproved): Tier-3 CSS injection/cross-stylesheet timing (reproduces with ZERO live DOM mutation, pure static single-stylesheet build), `var(--page-margin)` with no fallback, absence of a bare `@page citizen-file {}` rule, page count/depth (reproduces on a 10-page trimmed copy of the same book), and CSS/stylesheet complexity in isolation (a synthetic 2–10 page book using the identical rule shapes does not reproduce it). This is a genuine, narrow Chrome 151 native-print defect tied to named-page *transitions* (chapter-start → citizen-file is the book's first transition into this name), not a book CSS authoring error and not a Gutterpress compiler bug — `resolvePage()`/`counterStyleCss()` compute the correct merged content in every case checked. No fix landed; recommend the book route this specific running head through the generic `:right`/`:left` pseudo selectors (already proven reliable) instead of a compound name+`:right` selector, as a workaround, pending upstream Chromium investigation. |
| 08-08 | **A.7 margin chrome — root cause CORRECTED (review pass)** | p7 is painted with the **`@page chapter-start:right`** rules — the PRECEDING run's page name — whose declarations deliberately suppress the running head and blank the folio chips (`native-furniture.css` §4 sets `@bottom-left/right { content: "" }` for `chapter-start`, and `chapter-start` declares no `@top-*` at all). The margin boxes are therefore painting *correctly for the name Chromium assigned*; the defect is in the page-NAME assignment for the first page of the run, not in margin-box painting | n/a — polyfill unaffected | **<, root-caused** — SUPERSEDED (fixed by `fb60972`), see the 08-08 REVIEW A.7 row below | Four-way marker probe on a scratch copy of the book (`@page chapter-start:right/:left` and `@page citizen-file:right/:left` each given a distinct `@top-*` string): **p7 renders `CSRIGHT-MARK`** (chapter-start:right), p8 renders `CFLEFT-MARK` (citizen-file:left), p9 renders `CFRIGHT-MARK` (citizen-file:right). So p7 resolves to `chapter-start`, NOT to `citizen-file`. This supersedes the "content fails to paint" framing in the row above — that framing is consistent with the same observations but points at the wrong mechanism, and would send an implementer hunting a paint bug instead of a name-assignment bug. It also explains every earlier observation: the `citizen-file:right` marker did not paint on p7 because that rule never applied to p7, and the generic unnamed `:right` marker did paint because unnamed rules cascade into whatever name is in force. **"Narrow Chrome 151 defect" is NOT established:** two minimal repros printed with the same Chrome 151 binary (`@page A`/`@page B` named runs, the second with `break-before:page` + `break-inside:avoid` + boxes taller than the page — the exact shape `.page` uses in `page-templates.css`) both assign the new run's first page the CORRECT name. The trigger is still unidentified and is something about this book's structure, not named-page transitions in general. An outline probe (`.page.chapter-start{outline:lime}` / `.page.citizen-file{outline:magenta}`) shows a `chapter-start` fragment edge at the very top of p7 with `citizen-file`'s leading edge already on p6, which is consistent with Chromium naming a page after the first *fragment* on it (both minimal repros show an overflow remnant keeping the old name) — suggested, not proven. Workaround recommendation (pseudo-only selectors) is unchanged and still valid. |
| 08-08 | **A.3 float placement — root cause** | `.two-column .dc-alert{break-inside:auto}` and `column-fill:auto` (native-furniture.css §10/§13, both deliberate fixes for OTHER, taller callouts elsewhere) leave a SHORT two-item multicol flow (one paragraph + one callout) nowhere to balance; Chromium drops the second item full-width below instead of beside | `column-fill:balance` (unset by native-furniture.css) balances the 2-item flow into 2 columns, callout beside text | **<, root-caused, partial fix available** | The "float-right" framing was a false lead: `@callout .float-right` never reaches the DOM (`dimm-city-plugin.js`'s `@callout` handler reads only `variant=`/`label=`, silently drops any other class token — confirmed in `book.html`: `<div class="dc-alert dc-origin-callout">`, no `float-right` anywhere). This is pure `columns:2` multicol flow: `.section.two-column > h2:first-child` and its following `<p>` get `column-span:all` (dc-components.css:3275/3316), leaving exactly TWO items — the second paragraph and the callout — in the actual 2-column flow. Scoping native's `column-fill:auto` back to `balance` for just `.dc-citizen-walkthrough.two-column` (`native-furniture.css`) measurably restores side-by-side placement (verified render, `/tmp/wpA/floatfix2-p7-7.png`), but reintroduces a *different*, already-documented Chromium multicol defect on THIS narrower case — the callout card's own label and body split across the column boundary despite `break-inside:avoid` (visible mid-column heading orphan). Not landed: an unscoped balance reversion risks the "3 pages of pagination churn" the book's own comments say `column-fill:auto` was added to fix elsewhere, and the scoped exception needs one more iteration (an explicit height or stronger break guard on the callout) before it is a net improvement rather than a different defect. |
| 08-08 | C.14 preview↔print parity | 0 divergences, 5 fixtures | n/a | > | `native-parity-gate.ts`, **empty allowlist** |
| 08-08 | **C.15 page navigation — FIXED** | goToPage(18/30/34) all land correctly on a 34pp book at 1400px | works | = | live drive, `packages/cli/src/preview/nav-native.test.ts` (new regression test): root cause was `detectVisiblePage()` scanning by `top` only — every sheet in a native chapter-row shares the same `top` (rows lay out horizontally, not vertically), so the scroll-end handler always resolved to the last sheet of whatever row was vertically visible, overwriting the correct `currentPage` set synchronously by `goToPage()` itself. Fixed with a max-overlap-area (falling back to nearest-point) 2-D detector; also fixed a `goto()`/`currentPage()` 0-based/1-based inconsistency in the engine (`goto(34)` on a 34pp book used to report `currentPage()` 33) |
| 08-08 | C.15 view modes (single/two-up) — PARTIAL (corrected by review pass) | scroll-snap granularity ONLY: single mode snaps every page, two-up snaps on recto pages so a pair stays together. It does NOT change what is on screen — sheets stay exactly where `decorate.ts` painted them, so at one scroll position `view-single` and `view-two-column` render **byte-identical** screenshots (measured: same md5 at 1400×900, p20) and, at a fit zoom, four sheets are visible in "two-up" (measured: pages 21/22/23/24 at `setZoom(0.55)`, 1600px) | works — a real relayout: `iframe-styles.ts` reflows `.pagedjs_pages` to one column or wrapped two-column | **<** | Review-pass live drives. **The originally-recorded `=` did not hold, twice over.** (1) The claimed evidence — a mid-run screenshot showing "both full sheets side by side" — reproduces byte-identically in `view-single`; native always lays a chapter row out horizontally, so the screenshot demonstrated the row layout, not the view mode. (2) The scroll-snap itself was inert as shipped: `scroll-snap-type` was declared on `body.folio-stage`, but that element's `overflow:auto` propagates to the viewport and leaves `<body>` a non-scroll-container, so nothing snapped in any mode (measured: smooth scrolls settled exactly where asked, sheet left edges 152–300px off the viewport edge). Moving the declaration to `:root:has(> body.folio-stage)` makes it real (same probe: settles flush, left edge offset 0). (3) The two-up recto rule keyed off `nth-child(odd)` on the assumption that every `.folio-layer` starts on a recto — false on this very book, whose layers start at pages 1, 3, 5, 7 and **14**; now keyed off a `data-side` attribute written by `decorate.ts` from the 1-based page number (verified: 34/34 sheets correct, snap-align `start` on exactly the odd pages, including across the page-14 layer). Verdict stays `<` because native still cannot do what paged's two-up does — moving content is architecturally excluded (see `decorate.ts` draw()) |
| 08-08 | C.16 outline / source sync / context menu | 64 entries, correct pages | works | = | live drive: `getOutline`, `getVisibleSource`, `getContextTargetAt`, `getRectsFor` |
| 08-08 | **C.16 block identity (`ref`) — RESOLVED (dropped)** | n/a — `ref` removed from the wire contract | n/a — `ref` removed from the wire contract | = | Chose option (b): dropped `ref` from `getRectsFor`/`setEditMask`/`getContextTargetAt` on BOTH legs (protocol bumped v5→v6); `{chapter, range}` is the only target shape now. Justification: a source range is duplicated verbatim onto every split fragment on both engines (confirmed in `preview-interface.js`'s own comments), so it groups a Paged.js clone-set exactly as well as `data-ref` did, and it was the ONLY identity the native viewer ever had in the first place (it never clones, so it never minted a ref) — the `ref` field was pure duplication, not a second independent capability. Simpler than option (a) (stamping a synthetic native ref) because it deletes ~150 lines of ref-grouping machinery (`elementsByRef`/`resolveBlockGroup`) instead of adding a new native id scheme. Desktop `BlockOverlayController` re-anchor (`pageChanged`/`renderingComplete`) now always uses `{chapter, range}` and works identically on both engines — verified via the full `packages/desktop` test suite (2410 tests) plus `packages/cli`'s own protocol-v6 unit coverage in `pagedjs-interface.test.mjs` |
| 08-08 | E.22 html export (design guide) | 53pp, folios + running heads | DOM snapshot | = | served + driven headless; 53 sheets, 40+ margin boxes with real content |
| 08-08 | E.23 standalone drop-in | 2pp render fully | n/a | > | hand-authored Paged Media + one script; no Paged.js equivalent |
| 08-08 | F.24 bundle freshness | byte-identical rebuild | n/a | = | `--force` rebuild left `git status` empty |
| 08-08 | **F.25 desktop export w/o system Chromium** | **fails** | works (Electron Chromium) | **<** | native ignores the injected `pdfRenderer`; contradicts ADR 0002 |
| 08-08 | **F.25 root cause + decision (WP-C item 1)** | Electron 42.1.0 bundles Chromium **148.0.7778.97**; the engine's `REQUIRED_MILESTONE` (`engine/shared/cdp.ts`) is **151** — Electron's OWN bundled Chromium does not meet the native engine's floor. Option (a) "drive the native engine over Electron's CDP" is therefore not viable on the current Electron pin (measured, not assumed, per the work order). Decision: option (b) — keep the external-Chromium requirement, make the failure excellent. Implemented: `build-preflight.ts`'s new `verifyNativeChromiumMilestone()`, called from `build-runner.ts` right after quality gates and before rendering for `engine:"native"`, runs a real milestone check (launches the pooled browser, reads `Browser.version()`), not just a presence check — previously a resolved-but-too-old Chromium surfaced only deep inside `buildNativePdf`, after lint/validate AND the markdown render had already run. Deliberately not folded into `preflightBuildTools` itself (which runs before quality gates): that would force every native build to await Chromium's cold start before lint/validate even start, defeating `prewarmBrowser()`'s overlap — placed after gates instead, so the common failure case (a quality gate throwing) never pays for it. `cdp.ts`'s `connectChromium` error also no longer tells a pool-path caller to set `FOLIO_CHROMIUM` (that path resolves via `CHROMIUM_PATH`/`PUPPETEER_EXECUTABLE_PATH` instead — the old message did nothing). `chromium.ts`'s "desktop app includes its own browser" line (false for native) corrected. ADR 0002 amended (`.reviews/adr/0002-pdf-rendering-and-pure-js-tooling.md`, "2026-08-08 addendum"). This satisfies criterion 25 (early, actionable preflight) on BOTH the CLI and desktop (`ExportController.build()` calls the same `lib.runBuild`, so the same preflight runs before the desktop's pre-export sync gate even starts) — it does NOT close F.25 itself, which stays an accepted, documented architecture constraint, not a bug: native desktop PDF export requires an external Chromium 151+ until Electron's bundled Chromium reaches that milestone. | n/a | **< (documented)** | `bun packages/cli/src/cli.ts build /tmp/fbtest/book --engine native --out …` with `REQUIRED_MILESTONE` temporarily raised to 9999 fails in 0.58s, before the lint/validate/render lines print, with the corrected message naming `CHROMIUM_PATH`; reverted immediately, re-ran clean (native 1.0s, paged 1.9s, both produce valid PDFs). Desktop-shaped proof: `lib.runBuild({..., pdfRenderer: fakePdfRenderer})` against a copy of the book with `engine: native` in its manifest — `fakePdfRenderer` never called (confirms native still ignores an injected renderer, ADR-documented), output PDF page size 432×288pt exactly matches the book's `@page{size:6in 4in}` (6in×72=432pt, 4in×72=288pt) |
| 08-08 | 26. build wall-clock (design guide 53pp) | 1,975 ms | 3,534 ms | > | clean back-to-back; native 1.8x faster |
| 08-08 | 26. build wall-clock (field guide 302pp) | ~13 min | ~4 min | accepted | agreed exception; paged measured under load so its true time is lower |
| 08-08 | **C.13 hot reload** (field guide, edit chapter-00.md, wall-clock write→preview-shows-change) | 603, 655, 603 ms (avg 620) | 553, 473, 502 ms (avg 509) | ~ (native ~20% slower, not a `<` per se — not the wall-clock exception's PDF-build scope, and both land well under 1s) | 3 samples each; `/tmp` throwaway measurement script driving the real preview server (`startPreviewServer`) + Puppeteer against the `/` shell, timing `fs.writeFile` → the iframe's live DOM containing a fresh unique marker string. Same file-watcher/debounce path on both legs; the gap is native's own re-fragmentation (Chromium multicol layout) running client-side in the iframe on every reload where paged.js's incremental chapter-splice is comparatively lighter for this fixture |
| 08-08 (superseded, see next row) | ~~**C.13 hot reload, re-measured (WORK PACKAGE B item 2)**~~ | ~~avg 521~~ | ~~avg 451~~ | withdrawn | This row measured `spliceChapterNative()`, which the 08-08 REVIEW pass removed as unsound (see the row below and the Closed entry). Its paged number (avg 451ms) also failed to reproduce under independent re-measurement. Kept struck-through rather than deleted so the record shows what was claimed. |
| 08-08 (review) | **C.13 hot reload, INDEPENDENTLY re-measured** — 34pp field guide, edit `chapter-01.md`, wall-clock `fs.writeFileSync` → the change is visible in the VISIBLE `#gutterpress-active` iframe AND that iframe reports a settled pagination, 5 samples, 1400×900, one `google-chrome` + puppeteer-core process per leg | 998, 989, 1050, 974, 1016 ms (avg 998, median 989) — paged's incremental `spliceChapter()` path, confirmed engaged (active-iframe identity survived) | 508, 535, 523, 481, 500 ms (avg 509, median 508) — native's plain full-reload `swap()` path | **`>` — native is ~2x FASTER than paged on hot reload** | Two separate books (`/tmp/fg-hot-native`, `/tmp/fg-hot-paged`) so the two servers never contend on the same files. Earlier passes' numbers are not comparable: this probe reads ONLY the visible active iframe (an earlier version of this harness picked whichever frame had `[data-chapter-src]` first, which can be the HIDDEN double-buffer build frame, understating the full-reload leg) and additionally requires pagination to have settled, so it measures what an author actually waits for. Native wins WITHOUT any incremental splice — which is why the splice was removed rather than repaired. NOTE the paged leg paginates this book to 32 pages where native gives 34; that native-vs-paged delta is pre-existing and out of this row's scope. |
| 08-08 | **Cold-cache page-count determinism (WORK PACKAGE B item 1)** — field guide, fresh Chromium profile per cold load vs one profile reused for 4 warm loads, 1400×900 | 4/4 cold loads report 34 pages (was 33/34/34/34 before the fix) | n/a (paged was already deterministic) | `>` (bug fixed) | `fragment.ts`'s `fragmentDocument()` now kicks off `document.fonts.ready` + a per-`<img>` intrinsic-size wait (`load`/`error`, not `complete`-blocking `decode()`) alongside the stylesheet fetch, and awaits it before the first `measure()` call. Verified: 4 cold + 4 warm loads via real `google-chrome` + puppeteer-core against the actual preview server, `--user-data-dir` reset between each cold load. Before: 1013–1016ms/load, one of four cold loads read 33 pages. After: 1012–1016ms/load (no measurable regression — the wait races the stylesheet fetch, which already dominates), 34/34 on all 8 loads across two runs. First cut used `img.decode()` (mirroring the compiler's own width-offender check) and cost the hot-reload path dearly (~330-400ms of real per-navigation decode time on this book's 20 images) for no additional correctness — `complete`/`load` already guarantees `naturalWidth`/`naturalHeight`, which is all layout needs; switched to that and the hot-reload regression mostly disappeared (see the C.13 row above) |
| 08-08 (review) | **Page-count determinism, INDEPENDENTLY re-verified — and the "cold-cache" framing CORRECTED** — 34pp field guide, 1400×900, `Gutterpress.totalPages` read after mount and again 1.5s later | **PRE-FIX (`fb60972`, run from a throwaway git worktree): 33, 34, 33, 33 cold / 33, 33, 34, 33 warm — 6 of 8 loads WRONG** | **POST-FIX (`aab211f`): 34, 34, 34, 34 cold / 34, 34, 34, 34 warm — 8 of 8 correct** | `>` (bug real, fix real, confirmed by an independent reviewer) | Cold = a brand-new `--user-data-dir` per load (empty disk cache), warm = one profile, primed once, then 4 more navigations. Two conclusions the original report got wrong in one direction each: (1) the defect is **worse** than "cold-cache" — it fired on 3 of 4 WARM loads too, so it is a fonts/images-not-yet-measurable race, NOT a cache-population race, and the fix is correspondingly more valuable; (2) the load timings reported originally (~1013ms) did not reproduce here (~210–306ms) — a different measurement start point, not a discrepancy in the page counts, which are what this row asserts. |
| 08-08 | **B.8 PDF/X-1a and PDF/X-3 both build** (34pp field guide `/tmp/fg-proof-parent/field-guide`, and 53pp design guide) | both build, exit 0 | both build, exit 0 | = | `build … --engine <leg> --format pdfx --pdfx-flavor x1a\|x3 --skip-post-validate`. Four field-guide artifacts: native x1a 164,045,627 B / 34pp, native x3 164,045,593 B / 34pp, paged x1a 156,021,264 B / 32pp, paged x3 156,021,230 B / 32pp. Conformance keys read out of the raw PDF are identical per flavor on both legs: x1a → `/GTS_PDFXVersion (PDF/X-1:2001)` + `/GTS_PDFXConformance (PDF/X-1a:2001)` + `/OutputConditionIdentifier (CGATS TR001)`; x3 → `/GTS_PDFXVersion (PDF/X-3:2002)`, no Conformance key, `/OutputConditionIdentifier (CGATS21_CRPC1)`. `/Trapped /False`, PDF version 1.3 on all four. Same result on the design guide (`dgx1a-*`, `dgx3-*`). NOTE: the PDF/X stage is engine-agnostic by construction — `build-runner.ts` hands the engine's raw PDF to the same `convertToPdfxCmyk()` Ghostscript call on both legs — so parity here is expected; it is now measured rather than assumed. Environment: `qpdf` is NOT installed on this machine; it was extracted from the Ubuntu .deb into `/tmp/wpD/prefix` and put on PATH for every run in this block. Without it `--format pdfx` refuses to start (preflight, both legs alike) |
| 08-08 | **B.9 ICC profile embedded / OutputIntent** (field guide x1a+x3, design guide x3) | embedded, byte-identical to source | embedded, byte-identical to source | = | Parsed `/OutputIntents` → `/DestOutputProfile` stream out of the raw PDF and inflated it: 3,706,180 bytes, md5 `a75587a5d284…`, **exactly equal** to `packages/cli/profiles/CGATS21_CRPC1.icc` on all legs; stream dict `/N 4` (CMYK); OutputIntent dict has `/S /GTS_PDFX`, `/OutputCondition (CGATS21_CRPC1)`. The field guide's manifest points `pdfx.icc` at `.print-md/profiles/CGATS21_CRPC1.icc`, which does **not** exist in this checkout — both legs fall back to the embedded profile identically |
| 08-08 | **B.10 post-build validation** (34pp field guide, x1a) | FAILS: `No fonts detected (unexpected)` (1 error) | FAILS: `No fonts detected (unexpected)` (1 error) | = (both fail, identically) | `gutterpress validate /tmp/fg-proof-parent/field-guide --pdf <artifact> --phase post`, full output diffed: same info lines, same single escalated-to-error finding under the `dtrpg` target. Root cause is shared and NOT engine-specific — see the B.10-rasterization row below. On the 53pp design guide both legs **PASS** (`VALIDATION PASSED`, native: 20 link annots / 414 unique horizontal text positions across 2,593 blocks; paged: 21 / 407 / 2,662) |
| 08-08 | **B.10 PDF/X output is fully RASTERIZED on this book — shared defect, not a native regression** (34pp field guide) | 0 fonts, 0 extractable chars, every page one 6210×8100 CMYK 720ppi image | 0 fonts, 0 extractable chars, every page one 6211×8100 CMYK 720ppi image | = (parity; both bad) | `pdffonts` lists ZERO fonts and `pdftotext` extracts ZERO characters on all 34/32 pages of BOTH legs' PDF/X artifacts, while the same books' plain-PDF artifacts keep all 8–9 embedded fonts and 33.5K characters. `pdfimages -list -f 1 -l 3` shows one full-page CMYK image per page on both. This is Ghostscript flattening transparency down to `-dCompatibilityLevel=1.3` (`ghostscript.ts`'s fixed args, used by both legs), so it is engine-agnostic — it reproduces identically under `--engine paged` and therefore does not block the native migration, but it IS a real, pre-existing print-quality defect on this book (searchable text and vector type are lost in the deliverable that actually goes to the printer). The design guide does **not** trigger it (fonts + text survive on both legs), so the trigger is book content (transparency/blend modes), not the pipeline in general. Not fixed here — out of this work package's scope and it would change PDF/X output for both engines |
| 08-08 | **B.11 PDF boxes — MediaBox/TrimBox/BleedBox/ArtBox** (purpose-built fixture `/tmp/wpD/marksbook`: `@page{size:6in 9in; bleed:0.125in; marks:crop cross; margin:.5in}`) | Media 0,0,486,702; **Bleed 18,18,468,684**; **Trim 27,27,459,675** (= exactly 432×648pt = 6in×9in trim, + 9pt bleed, + 18pt slug) | Media 0,0,450,666; Bleed = Trim = Art = **MediaBox** (no box metadata at all); media is trim+2×bleed with **no slug** | **>** | `pdfinfo -box` on plain-PDF and on PDF/X output of the same fixture, both legs. Native's boxes are correct and **survive the Ghostscript PDF/X stage unchanged** (`marksx-native.pdf` reports the same Bleed/Trim). Paged sets no TrimBox/BleedBox — a printer receiving that file has no trim geometry. Native is better; recorded as a deliberate improvement, not a divergence to "fix". On books with no `bleed:` (field guide, design guide) both legs report all boxes = MediaBox, i.e. `=` |
| 08-08 | **B.11 crop marks** (same fixture, `marks: crop cross`) | drawn, full 18pt arms in the slug, offset from trim by the 9pt bleed | drawn but **clipped to 2.88pt stubs** at the media edge | **>** | Rendered p1 at 100 dpi both legs and LOOKED (`/tmp/wpD/marks-corners.png`, and post-PDF/X `/tmp/wpD/mx-corners.png`): native shows the expected L-pair at each trim corner; paged shows two truncated ticks running off the sheet, because paged's media box grows by bleed only and leaves no slug for marks to live in. Measured arm length by scanning the top-left mark row: native 25 dark px @100dpi = 18.0pt, paged 4 px = 2.88pt |
| 08-08 | **B.12 annotation stripping (`--strip-annotations`)** (53pp design guide, x3) | link annots NOT removed: 20 `/Subtype /Link` objects before and after | link annots NOT removed: 21 before and after | = (both ineffective) | Counted `/Subtype /Link` in the qpdf-normalised (`--qdf --object-streams=disable`) PDFs before and after. Both legs log `Stripping annotations for PDF/X compliance` and both still emit the Ghostscript warning `Annotation (not TrapNet or PrinterMark) on page`. `stripAnnotations()` runs `qpdf --flatten-annotations=all`, which flattens appearance streams but does not delete Link annotations. Shared, pre-existing, engine-agnostic — parity holds, but the flag does not do what its name promises on either leg |
| 08-08 | **B.12 metadata (title/author)** (field guide + fixture, pdfx) | `/Title` set from the manifest; `/Author` **never set**; `/Trapped /False` | identical: `/Title` set, `/Author` **never set**, `/Trapped /False` | = | `pdfinfo` on all four field-guide PDF/X artifacts and both fixture artifacts. Title arrives via the `pdfx_def.ps` DOCINFO pdfmark (engine-agnostic). Author is absent on both legs even though `manifest.yaml` declares `authors:` — on the native leg because `lib/engine.ts`'s `buildNativePdf()` calls `build({input, browser})` and passes none of the engine's `title`/`author`/`signature`/`marks` options; on the paged leg because nothing sets it at all. Not fixed here: wiring `author` through would change the paged leg's output too (it is a shared gap, not a parity gap). NB the PDF/X stage also overwrites plain-PDF's `/Creator (gutterpress)` with the Chromium UA string — again on both legs |
| 08-08 | **B.12 signature padding** | **UNTESTED — no CLI surface** | **UNTESTED — no CLI surface** | untested | `gutterpress build --help` exposes no `--signature` (nor `--marks`/`--bleed`) flag, and no manifest key feeds one: the engine implements it (`engine/compiler/postprocess.ts` pads to `opts.signature`) but `buildNativePdf()` never passes the option, and the Paged.js leg has no implementation at all. To test it, a fixture would need a manifest/CLI route to `BuildOptions.signature`; none exists today. Recorded as untested, NOT as parity |
| 08-08 | **B. PDF/X build wall-clock** (34pp field guide) | x1a 7m32s, x3 7m33s | x1a 4m27s, x3 4m27s | accepted (wall-clock exception) | `time` around each build; the Ghostscript CMYK stage dominates on both legs (the native plain-PDF build of the same book is ~1 min). Native's larger raw PDF (38.0 MB vs 35.3 MB) is what gs then has to chew through |
| 08-08 | **A.4 text extractability per page — 302pp real book** | 302pp, 145,449 chars, **3** text-free pages (6, 211, 215) | 303pp, 144,054 chars, **4** text-free pages (6, 44, 217, 221) | = (native slightly better) | `pdftotext` on `/tmp/wpA/fg-final-native.pdf` and `/tmp/wpA/fg-final-paged.pdf` (the same two artifacts work package A measured, page counts 302/303 reproduce), split on `\f`, whitespace-stripped per page. Every native text-free page was matched to its paged counterpart by fingerprinting the PRECEDING page's text (native 6→paged 6, native 211→paged 217, native 215→paged 221) — all three are text-free on the paged leg too, i.e. full-bleed art pages, not new rasterization. Paged has one EXTRA text-free page (its p44) with no native counterpart. **No page has text under paged and none under native.** |
| 08-08 | A.4 text extractability per page — 53pp design guide / 34pp field guide | 53pp, 41,178 chars, 0 text-free pages / 34pp, 33,558 chars, 1 text-free page (p6) | 54pp, 41,193 chars, 0 text-free / 32pp, 33,562 chars, 1 text-free (p6) | = | Same method, per page. The one text-free page (p6) is the same full-page plate on both legs |
| 08-08 | **A.5 font embedding — 302pp real book** | 13 fonts, **all `emb yes`, `sub yes`, `uni yes`** | 13 fonts, all embedded/subset | = | `pdffonts` both artifacts: identical name set (Lixdu Type3 + CID, TitilliumWeb Regular/Bold/Italic/BoldItalic, Tomorrow Regular/Bold/Italic/BoldItalic, NotoSansCJKjp, LiberationSans). No font is embedded on one leg and missing on the other, and no `emb no` row on either. LiberationSans appears on BOTH legs (a fallback for some glyph run) — so it is not a native-only substitution |
| 08-08 | A.5 font embedding — 53pp design guide / 34pp field guide | 15 / 8 fonts, all embedded+subset | 15 / 9 fonts, all embedded+subset | = | `pdffonts`. Design guide: identical 15-name set both legs (all Liberation\* + NotoSansMath — this machine has no webfonts for that book's stacks, equally on both legs). Field guide: same families, paged lists one extra `Lixdu` subset instance (3 vs 2) — a subsetting artifact, not a missing font. Caveat: `pdffonts` proves *embedding*, it cannot prove the embedded face is the one the CSS asked for; the two legs resolving to the same set is the parity evidence |
| 08-08 | **F.24 determinism (53pp design guide, native, 2 clean builds)** | deterministic | n/a | = | Two back-to-back `--engine native` builds: 53pp both, `pdftotext` output **byte-identical**, and all **53/53** pages rendered at 72 dpi with `pdftoppm` are md5-identical. Files differ by 3 bytes; normalising both with `qpdf --qdf --object-streams=disable` and diffing reduces the whole difference to 3 items: `/CreationDate`, `/ModDate`, and the file `/ID`. (One extra observation, unrelated to determinism: the design guide's PDF carries a link annotation whose `/URI` is `file:///tmp/gutterpress-build-<random>/guide.pdf` — the throwaway build directory leaks into the shipped artifact and changes every build. Present on this leg; not chased) |
| 08-08 | **F.24 determinism (34pp image-heavy field guide, native, 2 clean builds)** | deterministic | n/a | = | Same file size to the byte (37,979,690), 34pp both, `pdftotext` identical, pages 1/7/20/34 rendered at 50 dpi md5-identical. 3,061 raw bytes differ, all inside one FlateDecode `/Type /ObjStm`; inflating it shows the diff is **only** `/CreationDate` + `/ModDate`. Conclusion: native PDF output is reproducible modulo timestamps on both a text book and an image-heavy book |
| 08-08 (WP-C) | **A.3 float placement (field guide p7 aside) — FIXED** | callout sits beside its paragraph, whole, unsplit (2-column balance) | floats right beside text | **=** (closed; was `<`) | Root cause was two-fold, both fixed in `dc-design-guide/css/native-furniture.css` and `plugins/dimm-city-plugin.js`: (1) `column-fill: auto` (added earlier, correctly, for the FRAGMENTING multi-section `.two-column` wrapper on p12/p13) was also starving this SHORT single-`.section.two-column` flow of room to balance — scoped a `column-fill: balance` override to the exact instance (`.dc-citizen-walkthrough.two-column`, not a general `.section.two-column` rule, because p243's CORE ELEMENTS section is the same DOM shape but genuinely needs `auto` — the compiler's own multicol-fragmentation warning is the safety net for any other reuse); (2) restoring balance then let Chromium split the callout card's label from its body across the column gap despite `break-inside: avoid` — added a scoped `.dc-citizen-walkthrough.two-column .dc-alert { break-inside: avoid }` to override the existing (correctly-scoped-elsewhere) `.two-column .dc-alert { break-inside: auto }` rule for just this short instance. Verified: p7 rendered both legs, callout whole and beside the text; p12/p13 (the walkthrough this book's `column-fill: auto` was added to protect) re-rendered and still fill both columns with no dead column; `native-parity-gate.ts` still passes empty-allowlist on this fixture; 34pp, no multicol-fragmentation build warning. Separately: confirmed `@callout .float-right` never reaches the DOM — the plugin's `@callout` handler read only `variant=`/`label=` and silently dropped every other attribute, unlike its sibling `@block`/`@tape` handlers which do merge `attrs['class']`. Fixed for consistency (now merges `attrs['class']` the same way), though the class has no CSS effect today — no `.float-right` utility class exists anywhere in this book's CSS (only `.dc-img-float-right`), so the drop was never the actual cause of the layout difference; it was still a genuine, silently-dropped author-facing class token and is fixed on the same principle as the other markers. |
| 08-08 (WP-C) | **C.19-adjacent: PWA `readProjectEngine` manifest parsing — FIXED** | real `yaml` parse: honors quoted (`engine: "native"`), rejects commented-out and nested-under-another-key `engine:` lines, defaults `paged` on missing/malformed manifests | n/a (desktop-only, no paged-leg equivalent to compare) | n/a (correctness fix, not a native-vs-paged row) | `packages/desktop/src/lib/platform/web-adapter.ts`'s `readProjectEngine` used a regex (`/^engine:\s*["']?(paged\|native)["']?\s*$/m`) that silently defaulted to `paged` on any manifest shape it didn't match byte-for-byte — including a quoted value, which is valid YAML the CLI's real manifest loader honors. Replaced with `yaml`'s `parse()` (pure JS, ships a `browser` build, added as an explicit `packages/desktop` dependency — previously only reachable transitively through the `gutterpress` workspace dep) reading exactly the top-level `engine` key, so it stays PWA-clean (no `node:fs`/`node:path`, unlike the CLI's full `manifest.ts`). 10 new unit tests (`packages/desktop/tests/platform/web-adapter-engine.test.ts`) cover: plain native/paged, single- and double-quoted, commented-out (still defaults paged, correctly), nested under another key (still defaults paged, correctly), missing key, unrecognised value, missing manifest, malformed YAML (still defaults paged instead of throwing), and non-mapping documents (scalar/array). `npm run build`'s `check-render-purity` still passes (138 files scanned, no host/node markers in `build/client/`); full desktop suite (2424 tests) and CLI suite (2336 tests) both green. |
| 08-08 (WP-C) | **`/tmp/fbtest/book` full-bleed image placement — CLASSIFIED** | 2pp: text on p1 (`page: auto`), image forced onto its own p2 as a symmetric full-bleed (0 side margin, ~0.75in top/bottom retained) | 1pp: image renders inset inside the ORIGINAL page's 0.75in margins — not full-bleed at all, and no page break | **Paged.js bug; native is CSS-correct (`>`)** | Fixture CSS: `.full-bleed { page: gp-full-bleed; width:100%; max-width:none; margin-left:0; margin-right:0; }` where `@page gp-full-bleed { margin-left:0; margin-right:0; }`. Per the CSS Fragmentation / GCPM `page` property (named pages): when an element's used `page` value differs from the immediately preceding box's, a forced page break MUST occur between them. Native honors this exactly — text (implicit `page: auto`) stays on p1, the image (explicit `page: gp-full-bleed`) forces a break onto p2 and picks up the named page's zeroed side margins, rendering edge-to-edge horizontally with the untouched top/bottom margin still applied (measured, rendered both pages and looked: `/tmp/wpC/fbtest/native-{1,2}.jpg`). Paged.js does neither: no break is forced and the named page's margin override is never applied — the image sits fully inside the default margin box on the same single page, not bled to the edge on any side (measured, rendered: `/tmp/wpC/fbtest/paged-1.jpg`; not "inset left, bled off the right" as originally guessed — it is simply not full-bleed at all under Paged.js). This is Paged.js failing to implement the spec-mandated forced break on a `page:` value change, a known category of Paged.js polyfill gap; native's page count difference (2 vs 1) is a deliberate, CSS-correct improvement, not a defect to reconcile. |
| 08-08 (REVIEW of WP-C) | **A.7 margin chrome (chapter-opener pages) — RE-MEASURED, no longer reproduces** | p7 carries the running head `CITIZEN FILE`, the `C.1` chapter chip and the folio `P.7` | same three | **=** (closes the `<` at the two A.7 rows above and open blocker 2) | Independently rebuilt the 34pp field guide on both legs from the CURRENT committed trees (print-md `d500e47`, dc-op-manual `f2c7a79`) and rendered p7 at 72 dpi on each (`/tmp/rev/h-07.jpg` native, `/tmp/rev/p-07.jpg` paged): the chrome is present and matching. Also rendered p6 both legs — the chapter-start plate is chrome-free on both, which is the intended `chapter-start` suppression, not the bug. The bug was fixed by `fb60972` ("stop a lone image's line box from stealing the next page's @page rules") earlier on this branch; the WP-C sweep did not notice and left blocker 2 listed as open. Re-verified against three separate CSS variants (repo HEAD, `f2c7a79^`, and the stale `/tmp` fixture copy) — chrome present in all three, so it is the engine-side fix that closed it, not a book-CSS change. |
| 08-08 (REVIEW of WP-C) | **A.3 float placement (field guide p7 aside) — INDEPENDENTLY CONFIRMED, with a stronger p12 control** | p7: callout whole and beside its paragraph. p12: both columns filled, no dead column | p7: callout beside the text (same intent) | **=** (confirms the WP-C row above) | Rebuilt from the committed dc-op-manual tree at `f2c7a79` and at its parent `f2c7a79^`, into two isolated parent dirs, and rendered p7/p12/p13 at 72 dpi from each. **Pre-fix p7** (`/tmp/rev/b-07.jpg`): callout drops full-width BELOW the paragraph, right column dead — the reported defect reproduces. **Post-fix p7** (`/tmp/rev/h-07.jpg`): callout in the right column, whole, beside the paragraph; visually matches the paged leg's intent. **p12 is byte-identical pre- and post-fix** (same JPEG md5 `5a1dd44f…`), which proves the scoped selector did not touch the fragmenting walkthrough — stronger than "re-rendered and looked unchanged". NOTE for future runs: the WP-C verification was done against `/tmp/fg-proof-parent/dc-design-guide`, a COPY that had drifted from the repo (it was missing `21ee65a`/`1b4668f`); on that stale copy p12's right column is ~85% empty. The copy has now been resynced to the repo, and `native-parity-gate.ts` re-run against it (empty allowlist, 5/5 fixtures, 0 divergences). Separately, the `@callout` class merge added by `f2c7a79` interpolated the author's class token into the `class="…"` attribute UNESCAPED (its `@tape` sibling `esc()`s the same value); escaped in the follow-up, verified render-neutral (p7/p12/p13 JPEGs byte-identical before and after). |

| 08-09 | A.1 pagination after `body { margin: 0 }` (302pp field guide) | 301pp | 303pp | = | the PAGED_CSS body reset moved the real book 302 -> 301pp; type scale unchanged (18.252pt on pp 12/25/240, both builds), tokens 30,698 -> 30,710 (no loss), p12 columns and p244 CORE ELEMENTS render correctly. The gained 16px of measure per page reflows line breaking slightly and recovers one page — movement toward paged's own geometry, not away |

### 08-10 — D. CSS authoring effort, greenfield (new work package)

A brand-new 8-page book (`docs/fixtures/css-authoring-spike/book/`, committed as a
reproducible fixture) was built and styled from scratch, twice, against the
SAME visual target: chapters, a TOC with dot-leader `target-counter()` page
numbers, `string-set` running heads, folios, a floated image, a full-bleed
image, a two-column reference section, and a callout aside. Author surface
used was restricted to the user guide
(`examples/gutterpress-user-guide/`), standard Paged Media CSS, and core's
documented free classes (`.float-left`/`.float-right`/`.full-bleed`,
`.chapter-opener`). Method: write the obvious CSS once
(`styles/style.css`, shared), build both legs, render every page with
`pdftoppm`, look, and record any required deviation as an engine-specific
override file (`styles/style-native.css`, `styles/style-paged.css`).

| Date | Area | Native | Paged | Verdict | Evidence |
|---|---|---|---|---|---|
| 08-10 | **D.19/20 CSS authoring effort (greenfield 8pp book)** — **NUMBERS SUPERSEDED by the 08-10 REVIEW rows below (0 vs 19 lines was wrong; the real figures are 1 vs 12). Direction of the verdict survives, the margin does not.** | The obvious, spec-standard CSS (`style.css`, 95 real CSS lines) rendered the full target on the first build: dot-leader TOC via `leader(".") target-counter(attr(href), page)`, running heads via `string-set`, folios, floated image, two-column section, callout, full-bleed art. `style-native.css` stayed a 0-line stub — no engine-specific override needed anywhere in this book. Zero workarounds, zero engine-specific concepts, 1 build-and-look iteration (after fixing the test image's own aspect ratio, an asset-authoring detail equally relevant to both legs, not a CSS fix) | Same `style.css` as native, PLUS a required `style-paged.css` (19 real CSS lines) working around ONE broken primitive, and ONE remaining defect no CSS fix reaches at all (the full-bleed page, see next row). 5 build-and-look iterations were needed to isolate and fix the leader() failure (see next row) before the book matched intent | **native `>` paged** — native needed 0 workaround lines / 0 engine-specific concepts / 1 iteration; paged needed a 20% larger stylesheet, 1 hard workaround requiring a markup-adjacent CSS restructure (`position: relative` + two positioned pseudo-elements replacing one `leader()` term), and still ships with one uncorrectable visual gap | `docs/fixtures/css-authoring-spike/book/{manifest.yaml,manifest-paged.yaml,styles/style.css,styles/style-native.css,styles/style-paged.css}`; native.pdf 8pp / paged.pdf 7pp (`pdfinfo`); full-page renders compared at 100dpi, `pdftoppm`, side-by-side grids inspected for every page on both legs |
| 08-10 | **`leader()` is completely broken in the vendored Paged.js — not a syntax quirk, a dead primitive** | `leader(".") target-counter(attr(href), page)` in `.toc a::after` renders the full dot-leader + correct page number on the first try; plain `target-counter(attr(href))` (no `url` type keyword needed) resolves correctly against BOTH a chapter-wrapper id and a heading id | The IDENTICAL rule renders **nothing at all** — not just missing dots, the entire pseudo-element (page number included) goes blank. Isolated with a minimal 3-page repro (`/tmp/tc-test`, not committed — reproduce with the steps in Evidence): `content: " [" target-counter(attr(href), page) "]"` (no leader) renders `[1]`/`[2]` correctly; `content: leader(".") " done"` (leader, no target-counter at all) renders **nothing**. So `leader()` alone, with no other function involved, blanks whatever `content` list it appears in under this vendored Paged.js build — the textbook Paged Media TOC pattern (leader dots + page number) that the user guide's own hand-authored fixture uses (`packages/cli/src/engine/viewer/fixtures/standalone-hand.html`) is unusable as written. Also disproves a plausible alternate theory in passing: `attr(href url)` is NOT required for `target-counter()` on either leg — plain `attr(href)` already works on both, whenever `leader()` isn't in the same content list | **paged bug; native is correct (`>`)** | Fixture book's original `style-paged.css` first tried the `attr(href url)` form alone — reproduced the same total blank, ruling that out; only removing `leader()` fixed it. Workaround shipped in `style-paged.css`: absolutely-positioned `::before` dotted border + separately positioned `::after` for the number, gated behind `position: relative` on `.toc a`. Visually close but not equivalent — a real `leader()` proportionally fills to the next tab stop; the dotted-border fake always spans the same fixed inset regardless of link text length. Rendered and confirmed on the real fixture book: `/tmp/css-spike-out/final-p-grid.jpg` p2 shows dots + correct page numbers (3, 4) after the fix |
| 08-10 | ~~**Full-bleed image: still edge-to-edge only on native — reconfirmed on a brand-new minimal book**~~ — **WITHDRAWN 08-10 (REVIEW): the Paged half of this row does not reproduce; see the REVIEW rows below** | `.full-bleed` (core, free — no author CSS) bleeds edge-to-edge left/right exactly as documented; pixel scan of the rendered page (100dpi) shows ink from x=2 to x=599 of a 600px-wide page, i.e. essentially the full sheet width, with only the (expected, documented) top/bottom margins retained | The same core `.full-bleed` class, same markup, same CSS, leaves the image inset with a visible white margin on both left and right — the negative-margin `--pagedjs-margin-left/right` mechanism `PAGED_CSS` ships for this exact case (`markdown-it-paged.js`) does not engage; those custom properties evidently never get set to a nonzero value inside this book's DOM in this Paged.js build | **paged bug; native is correct (`>`), reconfirms the WP-C field-guide finding above on an independent, minimal fixture — no author-side workaround exists within documented CSS** (adding CSS could not close this gap; it is a Paged.js engine defect in the mechanism core itself relies on) | `docs/fixtures/css-authoring-spike/book/03-chapter-two.md`'s `.gallery-page`; rendered both legs at 100dpi, compared p7 (native) vs p6 (paged) in `/tmp/css-spike-out/final-n-grid.jpg` / `final-p-grid.jpg` |
| 08-10 | **printsafe `no-pagedjs-crash-selectors` — bites on plausible non-technical-author CSS, blocks the build (error, not warning) even for a native-only project** | N/A — the rule runs regardless of `--engine`; a `--engine native` build of CSS containing the flagged pattern still fails lint before rendering | N/A — same lint, same result | **noted friction, not a blocker** — matches item 4's premise: the rule protects Paged.js by banning a pattern native itself handles fine, and it can plausibly come up in ordinary authoring | Deliberately added `.callout:not(.warning) + .callout { margin-top: 1.2em; }` (a natural way to add spacing between adjacent callouts unless the first is a warning) to a scratch stylesheet and ran `gutterpress lint`: `error … Selector will be skipped by Paged.js … printsafe/no-pagedjs-crash-selectors`, exit nonzero. The real fixture book's own CSS never needed this pattern and lints clean (`CSS lint passed`) on both manifests. Not fixed/relaxed here — flagged as the "does it bite in practice" answer the task asked for: yes, it can, on ordinary CSS a non-technical author might reach for, and it costs native builds an otherwise-valid selector for Paged.js's sake |

#### 08-10 REVIEW of D (independent rebuild of both legs from the committed fixture)

Both legs rebuilt from scratch at commit `a3650e4` (`bun packages/cli/src/cli.ts
build … manifest.yaml` / `manifest-paged.yaml`), every page rendered
(`pdftoppm -r 72`/`-r 100`) and inspected side-by-side, plus per-page
`pdftotext` and per-row pixel scans. Three of the four D rows above needed
correcting; two previously-unrecorded defects (one per engine) turned up. The
fixture's two override stylesheets were rewritten to match what is actually
required, so the committed numbers are now reproducible.

| Date | Area | Native | Paged | Verdict | Evidence |
|---|---|---|---|---|---|
| 08-10 (REVIEW of D) | **Workaround line counts — RE-COUNTED, both were wrong** | **1 line, not 0.** The committed `style-native.css` was a 0-line stub, but the book it shipped with was NOT hitting its visual target on native: it printed 8pp with a **fully blank page 6** (0 dark pixels of 540,000 at 100 dpi) where the full-bleed plate should have been. The one-line author fix is `.gallery-page > p { margin: 0 }` → 7pp, plate on p6, bleed preserved | **12 lines, not 19.** The `leader()` workaround is genuinely required (proven below), but the committed version was ~40% larger than it needs to be: the absolutely-positioned `::before` dotted rule + `position:relative` + `display:inline-block; width:100%` collapses to a plain `border-bottom: 1pt dotted` on a `display:block` link with one positioned `::after`. Rendered head-to-head — the trimmed version is visually indistinguishable from (arguably cleaner than) the 19-line original | **native `>` paged still, but by 1 line vs 12, not 0 vs 19** — and each engine now carries one defect the other does not (next two rows), so "zero engine-specific concepts on native" is withdrawn | Both stylesheets rewritten in this commit with the disproof recorded in their header comments. Trimmed-vs-original TOC rendered at 100 dpi and compared against native's real leaders in one strip; both legs now build 7pp (`pdfinfo`) |
| 08-10 (REVIEW of D) | **NEW native defect: a `.full-bleed` image emits a spurious blank page** | A markdown image is always wrapped in a `<p>`, and core's `.full-bleed` sizes the art to the FULL page content box (7.5in on this 9in sheet). The `<p>`'s default 1em top margin overflows the box, the art is pushed to the next page, and native leaves a **blank sheet** behind — 8pp instead of 7pp, on stock core CSS with no author error involved. Bisected: `.full-width` instead of `.full-bleed` → 7pp; `.full-bleed { break-before: auto }` → still 8pp (so it is the overflow, not the double break); `.full-bleed { page: auto }` → 7pp but the bleed is lost (ink x=74..524 instead of 0..599); `.gallery-page > p { margin: 0 }` → 7pp **with** the bleed intact | Paged.js does not do this — it lets the art overflow the bottom trim instead (ink y=74..899 of 900, i.e. off the bottom edge; native keeps the documented y=74..824 margins). Neither is "correct", but paged's failure mode does not cost a sheet | **`<` (native worse)** — the author-side fix is one line, but every author who writes `![art](x.jpg){.full-bleed}` at full page height hits it, so this belongs in core (`PAGED_CSS` should zero the margin on a `p` wrapping a `.full-bleed`). NOT fixed here — measuring only; a core CSS change affects every book | `docs/fixtures/css-authoring-spike/book/{03-chapter-two.md,styles/style-native.css}`; `pdfinfo` page counts and `pdftotext -f 6 -l 6` (empty) for each of the four bisection variants |
| 08-10 (REVIEW of D) | **NEW paged defect: the TOC `target-counter()` page number is off by one** | `Chapter Two — A Foraging Gallery ...... 5`, and chapter two really is on page 5 (`pdftotext -f 5 -l 5`). Correct | `Chapter Two — A Foraging Gallery  4` — chapter two is on page **5** in the same PDF. Chapter One's `3` is correct, so it is not a uniform offset. Paged.js logs "rendered 8 pages" and the PDF has 7, so a page is being dropped after the counters resolve | **`>` (native correct, paged wrong)** — a wrong TOC page number is a silent correctness failure an author would ship | Both legs, `pdftotext -f 2 -l 2` vs `pdftotext -f 5 -l 5`; also visible in the rendered p2 strip |
| 08-10 (REVIEW of D) | **NEW native defect: `string()` running head is wrong on 2 of 7 pages** | p2 prints `THE SALT MARSH ALMANAC` (the cover's `h1`) though p2's own `h1` is `Contents`; p3 prints `CONTENTS` though p3's own `h1` is `Reading the Tides`. Per CSS GCPM, `string(x)` is `string(x, first)` = the first assignment **on that page**, so both are wrong. p4/p5/p7 are correct | Correct on all 7 pages: p2 `CONTENTS`, p3 `READING THE TIDES`, p4 `READING THE TIDES`, p5/p7 `A FORAGING GALLERY` | **`<` (native worse)** — this is the headline `string-set` feature the fixture was built to exercise, and the original D.19/20 row claimed native "rendered the full target on the first build". That claim is **withdrawn**. Not root-caused | Per-page `pdftotext` first line, both legs, printed in full above. Two obvious models DISPROVED: it is not a uniform one-page lag (p5 has its own `h1` and is correct), and it is not `string(x, start)`/entry-value semantics (p5's entry value would be `Reading the Tides`). Also not the named-page-transition family from the A.7 rows: deleting `[data-page="cover"] { page: cover }` leaves p2/p3 equally wrong |
| 08-10 (REVIEW of D) | **`leader()` broken under Paged.js — CONFIRMED on the fixture itself, not just a scratch repro** | Renders real dot leaders + correct numbers (`3`, `5`) | Deleting `styles/style-paged.css` from `manifest-paged.yaml` and rebuilding leaves the TOC with **no page numbers at all**: `pdftotext -f 2 -l 2` → "Chapter One — Reading the Tides Chapter Two — A Foraging Gallery" — the whole `::after` is blanked by the `leader()` term, exactly as the original row claimed | **paged bug; native correct (`>`)** — original row upheld | Rebuilt without the override file; output quoted above. The fake leader also underlines the link text (a real `leader()` starts after it), so the workaround is visually close but not equivalent — upheld |
| 08-10 (REVIEW of D) | **printsafe `no-pagedjs-crash-selectors` blocks a native-only build — CONFIRMED** | Reproduced independently | same | **noted friction; original row upheld** | Appended `.callout:not(.warning) + .callout { margin-top: 1.2em; }` to `style-native.css` and built the **native** manifest: `error  19:1  Selector will be skipped by Paged.js … (printsafe/no-pagedjs-crash-selectors)`, `error CSS lint errors found`, no PDF produced |

### Open blockers (verdict `<`)

1. ~~**Desktop PDF export requires a system Chromium** under native~~ — **CLOSED 2026-08-09** (see the "F.25 desktop export w/o system Chromium — CLOSED" row above). The premise behind this blocker (driving the engine over Electron's own CDP is not viable at Electron's bundled milestone) turned out to be only half true: Electron's *devtools-protocol* surface really can't host the engine (`Target.createTarget`/`Page.printToPDF` both fail on an attached session), but Electron's own `webContents` API (`executeJavaScript`, `debugger.sendCommand` for the `Emulation` domain, `printToPDF`) covers the exact same contract, and the 151 floor that made 148 look insufficient was never actually load-bearing — `generatedContentCss()`'s out-specifying override survives both the pre-151 and 151+ regime. `packages/desktop/electron/engine-browser.ts` implements the engine's `Browser`/`Session` interfaces this way; `REQUIRED_MILESTONE` moved to 148 (Electron 42.1.0's real bundled version); `engine.ts`/`build-runner.ts` grew an `engineBrowser` injection seam. Verified end-to-end with no external Chromium reachable (`PATH` masked, no `*_CHROMIUM*`/`PUPPETEER_EXECUTABLE_PATH` env set): 34pp field guide and 53pp design guide both build correctly, page size and named-destination page map identical to the external-Chromium build. An author on a machine with no external Chromium can now export native PDFs from the desktop exactly like the Paged.js leg.
2. **PDF/X output is fully rasterized on the field guide — on BOTH engines** (found 08-08, work package D). `--format pdfx` on that book yields zero embedded fonts and zero extractable text; every page becomes one 720 ppi CMYK image, and post-build validation fails with `No fonts detected (unexpected)` — identically under `--engine paged`. So it is **not** a native blocker (verdict `=`) and does not gate the migration, but it is a real print-quality defect in the shipped PDF/X pipeline: Ghostscript flattening transparency to `-dCompatibilityLevel=1.3`. The design guide is unaffected on both legs, so the trigger is book content. Listed here so it is not lost — owner: the PDF/X pipeline, not the engine.
3. **`Gutterpress.refresh()`/`relayout()` cannot absorb a page-context change** (found in the 08-08 REVIEW pass; PRE-EXISTING engine limitation, now documented rather than fixed). `fragment.ts`'s `relayout()` only re-`measure()`s the strip list built at mount — it never re-runs `buildStrips()`/`explodeChildren()`. So any DOM mutation that introduces a NEW named-page assignment is invisible to a refresh. Measured on a purpose-built 2-chapter fixture (`/tmp/gp-splice`): appending `<div class="on-named">` (with `.on-named { page: named }`) to a chapter and refreshing left the viewer at **2 pages**, where a full reload of the identical content gives **3**, and `build --engine native` prints **3** — i.e. the refresh result was the wrong one. This is why the native incremental preview splice was removed (see Closed, below): it was the only caller that mutated the DOM and then relied on `refresh()`. Nothing in the shipped product hits this today (the preview's every other path does a full reload), but any future incremental-update work MUST rebuild strips first.
4. **Two-up/spread view** — REOPENED AS TRACTABLE (2026-08-09), superseding the "closed as permanent" ruling below it. The permanence claim rested on "a real relayout needs per-page DOM chunking, which this engine refuses to do". That is now false: **`column-wrap: wrap` + `column-height` (CSS Multicol L2) shipped unflagged in Chrome/Edge 145 (14 Jan 2026)** and lays multicol columns out in WRAPPING ROWS. Prototyped on both real books: setting `column-height: <content-h>; column-wrap: wrap` and a 2-column width on each `.folio-strip` after mount produces a true vertically-scrolling spread grid where sheets and content move together (they are the same box). MEASURED — the fragment index of EVERY element, before vs after: design-guide 1677 elements / 53pp → 53pp / **0 mismatches**; field-guide 842 elements / 34pp → 34pp / **0 mismatches**. Wrapping changes where columns are PLACED, not where content BREAKS — the fragmenter still runs once, no chunking, no cloning, so the parity gate's contract is untouched by construction. Toggle cost 1.6–23ms first / ~2ms after, against a 67–83ms mount. Availability confirmed: system Chrome 151 supports both properties (`CSS.supports` → true/true) and **Electron 42.1.0 bundles Chromium 148 ≥ 145**, so the desktop can use it.
   NOTE FOR THE CONSTITUTION: the relaxation of "tooling code need not be standards-replaceable" turned out NOT to be needed here — the capability arrived as a real, shipped standard. "It never chunks the DOM" stands, now on measured merit rather than principle.
   CROSS-RUN PAIRING — SHIPPED 2026-08-10. Investigated in order per the assigned task: (1) does PRINT already insert recto/verso blanks that the viewer should just re-present? No — neither book declares `break-before: recto|verso|left|right` anywhere (`grep` came up empty on both stylesheets), so `planRectoBlanks` never fires for these books; there is no existing print decision to diverge from. (2) Can consecutive runs share ONE wrap grid (a global container), or be phase-shifted with NO padding? Tested a `direction: rtl` mirror trick first (verified empirically: it correctly reverses which physical edge a column paints at, with 0 effect on fragment break offsets — a real, working technique) but it does not solve the actual problem: multicol's ROW GROUPING (which fragments land in the same wrapped row) is fixed by sequential fill order and is untouched by `direction` — RTL only mirrors left/right *within* a row already-grouped by the browser. The real defect was groupING (a recto-starting run's first two fragments — a recto and the FOLLOWING verso, which are never a real spread pair — were landing in the same row), not placement, so a same-box CSS-only mirror cannot fix it. A single shared wrap grid across runs was ruled out for a different, structural reason: `column-height` is one value per multicol box, and named pages in these books genuinely have different content heights (e.g. the design guide's `chapter` page adds `margin-top: 2.5in`) — merging would silently change fragmentation, which the parity gate exists to catch. (3) Implemented the explicitly-scoped fallback: a real, zero-height, zero-content `.folio-wrap-spacer` (`break-after: column`) is inserted as a run's first child, ONLY when that run's first physical page is a recto, AFTER `measure()` has already fixed `strip.pages`/`offset`/`totalPages` for good — so it is pure grid-slot bookkeeping and can never perturb the page count or any page-of-element mapping (`indexInStrip` subtracts the shift back out; `unwrapStrips` removes it before every `relayout()` rebuild so it can never leak into the flow root as if it were authored content). MEASURED cross-run outcome on both real books (`applySpreadMode(strips, true)`, headless Chrome, geometry read back from `getBoundingClientRect()`): every 2-sheet row is a true consecutive `(verso, recto)` pair at equal `top`; every solo row (necessarily at a run boundary, since two different runs are two different DOM boxes and can never literally share one row) is correctly sided — recto alone on the right, verso alone on the left — reproduced across the first 7 run boundaries on the field guide and the first 8 on the design guide, with 0 mismatches. Parity gate: 0 divergences, empty allowlist, page counts unchanged (design guide 53pp, field guide 34pp). CSS.supports gate verified both ways: real support (`column-wrap`/`column-height` both `true`) renders the grid; stubbing `CSS.supports` to return `false` for `column-wrap` renders the pre-existing single-row fallback with no `data-wrap` attribute anywhere. Wired to the existing view-mode control: `preview-interface.js`'s `setViewMode()` now calls `window.Gutterpress.setSpread(mode !== 'single')` under the native engine (previously inert, only touched scroll-snap classes); `index.ts` exposes `FolioApi.setSpread(on)` and re-applies the last-requested mode after every `refresh()`/`relayout()`, since a rebuilt strip list starts unwrapped. Honest limitation, unchanged by this work and inherent to "one strip per named-page run, never chunk the DOM": a verso ending one run and a recto starting the next are never shown in the same physical row (two different multicol boxes cannot occupy one row) — each renders alone, correctly sided, stacked one above the other, which is the correctness ceiling this architecture can reach without becoming a per-page DOM chunker.

   ROW-PITCH CORRECTION (found in the 08-10 REVIEW pass of the above, fixed same day). The pairing measurement quoted above was real but INCOMPLETE: it read back `.folio-sheet` geometry only, so it proved the sheet CHROME was grouped and sided correctly and said nothing about where Chromium actually put the author's content. It did not. `rowStrideOf()` computed the vertical pitch between wrapped rows as `--folio-page-h + row-gap`, but multicol lays a wrapped row out at `column-height + row-gap`, and `column-height` is set to `--folio-content-h`. The formula therefore overshot by (margin-top + margin-bottom) — 180px on the design guide — CUMULATIVELY, once per row. MEASURED on the design guide with the sheets and the content probed together: sheets painted at a 1260px pitch while content wrapped at 1080px, so from row 1 onward the sheet sat below the text it was supposed to frame and drifted a further 180px each row; 82 of 363 probe elements (headings/paragraphs/list items) rendered fully OUTSIDE the sheet drawn for their own page, and `pageOf()` disagreed with its own single-mode answer on 106 of 363 — i.e. the exact "chrome moved, content stayed" failure the retired chrome-only two-up was rejected for, reintroduced through the row axis. The fix is one line: `rowStrideOf` reads `--folio-content-h` (the value `column-height` is actually set to), making it the exact mirror of `strideOf`'s `--folio-content-w + column-gap`. RE-MEASURED after the fix, both books, with content and chrome checked together: design guide 363/363 elements inside their own sheet and 363/363 `pageOf()` identical between single and spread mode; field guide 362/362 and 362/362. Screenshots inspected full-sheet (not just the top of the viewport): a mid-run spread (p18 verso | p19 recto) shows the running head, body text and folio landing correctly on each of the two sheets with the text continuing across the pair; a chapter-opener boundary shows p24|p25 paired, then the opener p26 alone in the LEFT slot with the right slot empty, then p27 alone on the right; p1 sits alone in the RIGHT slot with an empty left slot. LESSON, worth keeping: a two-up check that reads only sheet rectangles cannot detect this class of defect — every future spread/view-mode measurement must assert that CONTENT rects lie inside the sheet rect for the page the (parity-gated) single-mode `pageOf()` assigns them to.

~~5. **Core's `.full-bleed` primitive does not bleed under native**~~ — FIXED
   2026-08-09 (Work Package A), moved to Closed below.

### Closed since last iteration

- **Core's `.full-bleed` primitive does not bleed under native** — FIXED
  2026-08-09 (Work Package A). `PAGED_CSS`'s `.full-bleed` now carries
  `page: gp-full-bleed` plus a core-owned `@page gp-full-bleed { margin-left:
  0; margin-right: 0; }`, alongside the pre-existing `--pagedjs-margin-*`
  out-dent (kept, not deleted, so the Paged.js leg is byte-identical). Native
  honours the named page (content box = sheet, so `width: 100%` bleeds and
  nothing out-dents, so no shrink-to-fit trigger); Paged.js ignores the named
  page (unsupported) and keeps using the out-dent — the two mechanisms
  coexist without conflict. MEASURED (minimal fixture, 6×4in sheet, 0.75in
  margins, `body { margin: 0 }`, no book-level workaround CSS): ink extent on
  the bleed page at 300dpi — native 0.00–5.99in, paged 0.00–5.99in on a 6in
  sheet (both reach the edges; the fixture's own margin dimensions of the
  6in canvas round to 5.99 not 6.00 due to a 1px sampling threshold, not a
  gap). Text-run width probe on the SAME build (a fixed line of text on the
  non-bleed page): 2.0767in on both legs — identical, confirming no
  shrink-to-fit fired. Page counts unchanged on `/tmp/fbtest/book` (2pp
  native / 1pp paged — pre-existing Paged.js named-page bug, unrelated to
  this fix, already classified above), field-guide (34pp), and design-guide
  AS SHIPPED (53pp) — none of the three real books use core's `.full-bleed`
  class, so none move.
  **Correction to the original fix plan's "free" framing:** adding an
  ACTUAL `.full-bleed` image mid-chapter to a real book (design-guide +
  one synthetic image in `02-palette.md`) does change pagination: 53pp base
  → 54pp with the OLD (non-bleeding) `.full-bleed` → **55pp** with the fix,
  a genuine +1 page versus the old behavior, reproduced whether or not the
  image was styled to fill the page's full height. Rendered and looked at
  both: under the old CSS the (non-bleeding, inset) image and the next
  heading shared one page; under the fix the image gets its own page and
  the next heading is forced to a fresh page — this is the CSS
  Fragmentation spec's mandatory forced break when a box's used `page`
  value changes (here: image on `gp-full-bleed` → next content back on
  `auto`), not a bug. The "adds only a break-after, which is free on an
  already-full art page" claim does not hold in general — it is free only
  when the following content already had nowhere else to go on that sheet,
  which was not true in this test. Net effect: turning on true bleed for an
  existing `.full-bleed` image is a genuine, correct, +1-page repagination
  for that book, not a no-op — authors should expect this when adopting the
  fix on a book that already uses `.full-bleed`.
  Guard proven, not assumed: `findWidthOffenders()`'s per-context
  `Math.max` now excludes `gp-full-bleed` by name
  (`packages/cli/src/engine/compiler/build.ts`), so the shrink-to-fit
  hard-error limit stays pinned to the real content box (432px on the
  6×4in/0.75in fixture) instead of rising to the full sheet (576px) for
  every book. Reproduced both states on a fixture with a deliberately
  5.5in-wide div on that 6in/0.75in-margin book: with the exclusion, build
  **errors** — `div.overwide — 528px > 432px content box`; with the
  exclusion reverted (guard code temporarily stashed, CSS fix kept), the
  SAME build **succeeds silently** — exactly the project-wide masking bug
  the companion fix exists to prevent.
  Viewer agrees with print: mounted the preview, measured `img.full-bleed`'s
  `getBoundingClientRect()` against its containing `.folio-strip` — width
  576px vs strip width 576px, left/right edges coincide exactly (0 gap).
  The viewer needed no special-casing: `fragment.ts` already reads named-page
  geometry through the same shared `resolvePage`/`extract` used by the
  compiler, so `gp-full-bleed` is handled generically.
  Suites: CLI `bun test` 2345 pass / 1 skip / 0 fail; desktop `npm test`
  2424 pass / 0 fail; `bun scripts/native-parity-gate.ts` with an empty
  `KNOWN_DIVERGENCES`: 5/5 fixtures, 0 divergences, gate PASSES.
  **08-09 REVIEW PASS — the measurement above was real but its fixture
  carried `body { margin: 0 }`, and that turned out to be load-bearing.**
  Re-measured on a fixture WITHOUT it (which is what a default book is):
  the bleed page's ink spanned **0.080–5.917in of a 6in sheet**, not
  0.000–6.000in — an 8px-per-side white frame, confirmed visually on the
  300dpi raster. Root-caused, not guessed: `width: 100%` resolves against
  the BODY box, and the UA default `body { margin: 8px }` survives native
  print while Paged.js's polisher drops it; re-adding `body { margin: 0 }`
  to the same fixture took it to exactly 0.000–6.000in. Fixed in core —
  `PAGED_CSS` now zeroes the body margin (first in the cascade, so authors
  still override it). After the fix, on the no-reset fixture: bleed page
  0.000–6.000in (native) and 0.000–6.000in (paged), and the non-bleed
  page's text run moved from 0.840–2.920in to 0.757–2.837in, now exactly
  matching paged — the reset also removed an 8px-per-side inset native had
  been applying to every page of such a book. Paged.js leg re-measured with
  and without the named-page assignment: 2pp, page-2 ink 0.000–6.000in,
  page-1 ink 0.757–2.837in in BOTH — the "Paged.js output is unchanged"
  claim holds. Page counts after the reset: `/tmp/fbtest/book` 2pp,
  field-guide 34pp, design-guide 53pp — unchanged. Regression test with a
  proven negative control (deleting the rule fails it with `8px`):
  `packages/cli/src/lib/markdown/paged-css-full-bleed.test.ts`.
  Independently re-verified in the same review pass: design-guide 53pp base
  → 54pp old-behavior → 55pp fixed (reproduced exactly), the width-guard in
  BOTH states (with the `gp-full-bleed` exclusion the over-wide fixture
  errors at `div.toowide — 528px > 432px content box`; with the exclusion
  removed the identical build succeeds silently), and the running-head/folio
  trim-line gap, seen on design-guide's rendered bleed page.
  Known, documented gap (not fixed): on the bleed page, native's running
  head/folio move onto the trim line (margin boxes are positioned by the
  page's own now-zero margins). Not addressed in core — see
  `docs/native-engine-styling-guide.md` §9 for the one-line author
  suppression on `@page gp-full-bleed`.

- **Image XObject count** — re-measured on the real 302pp book. The originally-reported "3,067 vs 579" gap is a placement-count artifact, not duplicated bitmap data: grouping by object id gives 347 (native) vs 293 (paged) UNIQUE image objects, and summing each object's own bytes gives 149.9 MB vs 148.0 MB (+1.3%) of actual embedded image data — full PDF 167.5 MB vs 162.1 MB (+3.3%). (The unique-object counts and PDF sizes were re-measured and reproduce exactly; the byte figures originally recorded here as 36.8/35.3 MB did not reproduce and are corrected above.) The brick-tile XObject is confirmed SHARED (one object id, 43, placed 598 times) not re-encoded. Verdict corrected from `<` to `=`; the elevated row count is inherent to native's per-page margin-box painting model (16 boxes × 302 pages each need their own `Do` reference) versus Paged.js's continuous-canvas compositing, and has no meaningful file-size cost.
- **Preview page navigation saturation (C.15)** — fixed. `detectVisiblePage()` now handles the native 2-D chapter-row layout (max-overlap-area, falling back to nearest-point); `Gutterpress.goto()`/`currentPage()`'s 0-based/1-based mismatch fixed too. Regression-tested (`packages/cli/src/preview/nav-native.test.ts`).
- ~~**View modes (C.15)**~~ — NOT closed. Moved back to the C.15 row above as `<`: the shipped scroll-snap was inert (declared on a non-scroll-container) and its recto rule was keyed off a DOM-order assumption this book violates; both fixed in the review pass, but even fixed it is a snap-granularity feature, not paged's relayout.
- **Block identity `ref` (C.16)** — resolved by dropping it from the wire contract entirely (option b); `{chapter, range}` is the sole identity on both engines now (protocol v6). Desktop block-overlay re-anchor verified via the full test suite.
- **Native's incremental preview splice (`spliceChapterNative()`, WORK PACKAGE B item 2)** — REMOVED in the 08-08 review pass, not repaired. Three measured reasons: (a) it is UNSOUND — it grafts a fresh chapter node and calls `Gutterpress.refresh()`, which cannot absorb a new page context (open blocker 3 above: 2 preview pages vs 3 printed pages on a fixture where it genuinely engaged), a preview↔PDF divergence; (b) it is UNNECESSARY — independently re-measured end-to-end, native's plain full reload is ~2x faster than the paged leg's splice (509ms vs 998ms avg, see the C.13 review row), so the gate's hot-reload comparison is already a `>` for native without it; (c) it was UNREACHABLE on both real books anyway. `preview-shell.js` is back to the pre-work-package behaviour (paged splices, native full-reloads) plus a comment recording the above. The desktop regression test that covered it was removed with it.
- **Cold-cache page-count non-determinism (blocker #3, WORK PACKAGE B item 1)** — fixed. `fragment.ts`'s `fragmentDocument()` awaits web fonts and per-image intrinsic size before its first `measure()` pass. See the new C.13-adjacent row above for numbers; verified deterministic (34/34 pages) across 8 loads (4 cold-profile, 4 warm) with no measurable timing regression.
- **A.7 margin chrome on the first page of a named-page run (was open blocker 2)** — CLOSED in the 08-08 REVIEW pass; it was already fixed by `fb60972` on this branch and the WP-C doc sweep failed to close it. Root cause and fix are in that commit (a full-page image's inline line box added half-leading, pushing the named-page wrapper a few px past the content box so its trailing fragment landed on the next sheet, and a sheet whose first box is the previous page's fragment takes the previous page's name — `vertical-align: bottom` in `PAGED_CSS` collapses the line box). That is exactly the "named after its first fragment" mechanism the root-cause row above suspected but could not prove; the rows at A.7 above are superseded and kept only as the investigation trail. Re-measured in the review pass — see the new A.7 row for the observation.
- **A.3 float placement (field guide p7 "Image Is Everything" aside, WP-C item 1)** — fixed. Scoped `column-fill: balance` and a scoped `break-inside: avoid` restore for the callout, both keyed to the exact instance (`.dc-citizen-walkthrough.two-column`) rather than a general DOM-shape rule, since the same shape (`.section.two-column`) is reused by p243's CORE ELEMENTS section with genuinely long content that still needs `auto`/fragmenting. p7 now matches the paged leg's intent (callout whole, beside the text); p12/p13's fragmenting walkthrough (the original reason `column-fill: auto` was added) re-verified unaffected. See the new row above for the full evidence trail. The `@callout` class-drop was a real, separate plugin bug (fixed for consistency with `@block`/`@tape`) but was NOT the cause of this layout difference — no `.float-right` CSS rule exists in this book at all.

| 08-09 (WORK PACKAGE A) | **F.25 desktop export w/o system Chromium — RE-CONFIRMED, no new work needed** | still requires external Chromium 151+; `verifyNativeChromiumMilestone()` preflight already lands the honest-early-failure fix | works (Electron Chromium) | **< (documented, unchanged)** | Re-read the current tree: `packages/desktop/node_modules/electron` is pinned `42.1.0` (`^42.1.0` in `package.json`), matching the 148.0.7778.97-bundled-Chromium measurement already recorded in the F.25 rows above and in `.reviews/adr/0002-pdf-rendering-and-pure-js-tooling.md`'s 2026-08-08 addendum. `REQUIRED_MILESTONE` in `packages/cli/src/engine/shared/cdp.ts` is still `151`. Nothing changed the inputs to this decision since the last pass (Electron not rebumped, `REQUIRED_MILESTONE` not lowered), so option (b) — external Chromium required, fail early and honestly via `verifyNativeChromiumMilestone()` (confirmed still wired into `build-runner.ts`) — stands without modification. Re-verifying inputs rather than re-doing the work per this session's instructions ("FIRST MEASURE... report both numbers"). |
| 08-09 (WORK PACKAGE A) | **C.15 two-up/spread — RE-EVALUATED against `explodeChildren`, still `<`, now root-caused precisely and closed as a permanent architectural boundary, not a to-do** | scroll-snap granularity only, unchanged | works — real relayout | **< (final)** | Live-drove the 34pp field guide's native preview (`startPreviewServer`, puppeteer, 1400×900), landed on a mid-run pair (pages 20/21 — a verso/recto pair, NOT page 1), and toggled `body.view-single` ↔ `body.view-two-column` with no scroll between. Both modes report **byte-identical** sheet geometry (`{page:"20",left:0,right:828}`, `{page:"21",left:852,right:1680}` in both), confirming the class toggle alone changes nothing about what's on screen — it only changes future `scroll-snap-align` behavior, exactly the 08-08 C.15 finding, reproduced independently a day later. `explodeChildren` (`fragment.ts`) does NOT give this new capability: it only splits a `.folio-strip` at **named-page boundaries** (a `page:` property change between elements), which is a document-structure event, not a per-physical-page event — most of a book (and this fixture, pages 20/21) is one single named-page run spanning many physical pages, so `explodeChildren` emits exactly one strip for the whole run and the multicol-column-per-page problem `decorate.ts`'s own comment describes (lines 274-283: "the strip is one multicol flow element with N columns, not N independently movable elements... Fixing that for real would mean fragmenting the DOM per physical page — cloning content across pages — which is exactly what this engine exists to avoid") is untouched. Making sheets independently positionable (gaps between spread pairs, pairs that travel together as a unit) requires the CONTENT of one physical page to live in its own DOM box; CSS multicol offers no API to read "what's laid out in column N" except element rects (already used for `pageOf`/`pageRangeOf`), and CSS provides no way to vary `column-gap` between specific column pairs. The only way to get independently-movable per-page boxes is to split element content at each fragment boundary — i.e. become a per-page DOM chunker, which is the Paged.js clone-based model `fragment.ts`'s own module doc says this engine exists to NOT be ("Folio contributes ONE shallow wrapper per named-page run... It never chunks the DOM"). Conclusion: infeasible without abandoning the engine's core no-clone architecture. Retired as a permanent capability gap versus Paged.js, not a pending item — closing out the "if per-page strip segmentation is feasible, do it" instruction with a measured "no." |
| 08-09 (WORK PACKAGE B, desktop export) | **F.25 desktop export w/o system Chromium — CLOSED** | works (Electron's own bundled Chromium, no external browser) | works (Electron Chromium) | **=** | A prior investigation (WP-B pre-pass) had measured all three premises behind the "requires 151+" doctrine were stale or false: (1) the 151-pin's own motivating incident (`target-counter()` parses-but-drops-to-`none` at 151) is survived in BOTH the pre-151 and 151+ regime by `generatedContentCss()` out-specifying the author's selector — proven by re-running the spike suite on 148 vs 151 (2 differing checks, both assertions ABOUT Chromium's parse behavior, now loosened to accept either regime — `spike/folio/spikes/s0-native-baseline.ts`, `s2-cssom-page-rules.ts`) and finding the parity gate byte-identical and real book builds page-count/size-identical across both milestones; (2) Electron's CDP cannot host the engine at all (`Target.createTarget`/`Page.printToPDF` both fail on an attached Electron devtools session, so `connectChromium(wsEndpoint)` was never viable regardless of milestone); (3) Electron's OWN `webContents` API (not CDP) covers everything the engine needs. Implemented this session: `REQUIRED_MILESTONE` dropped 151→148 (`cdp.ts`) — 148 is Electron 42.1.0's actual bundled Chromium and the honestly-supported floor, not a relaxation; `packages/desktop/electron/engine-browser.ts` implements the engine's `Browser`/`Session` contract over a hidden `BrowserWindow` per page (`webContents.executeJavaScript` for `evaluate`, `webContents.debugger.sendCommand` for `Emulation.*`, `webContents.printToPDF` for `printToPDF`); `engine.ts`'s `buildNativePdf()` and `build-runner.ts`'s `BuildRunnerOptions` grew an optional `engineBrowser` factory seam (mirroring the existing `pdfRenderer` seam) so the desktop supplies its Electron browser and the CLI's pooled-external-Chromium path is untouched when it's omitted; `build-preflight.ts`'s Chromium presence check and `build-runner.ts`'s `verifyNativeChromiumMilestone()` both skip when `engineBrowser` is supplied, since that path drives its own browser and needs neither. Font-rendering residual risk (CDP launches with `--font-render-hinting=none --disable-gpu`, a bare `BrowserWindow` gets neither) was MEASURED, not left open: identical serif-heavy HTML printed to PDF with those switches forced on vs. off (via `app.commandLine.appendSwitch`) on this Electron 42.1.0/Chromium 148 produced byte-identical rasters (only the `/CreationDate`/`/ModDate` timestamp differed in the raw PDF bytes) — so the switches are deliberately NOT added (`disable-gpu` is app-wide and would cost every other window's compositing for no measured export benefit); see `engine-browser.ts`'s header doc for the full method and the standing caveat if a future report shows real-GPU divergence. | Desktop-shaped proof, run for real (not simulated): a `BrowserWindow`-hosted Electron main process ran `lib.runBuild({..., engine: "native", engineBrowser: createElectronEngineBrowser})` against the 34pp field guide and the 53pp design guide, with `PATH` masked to a nonexistent directory and `FOLIO_CHROMIUM`/`CHROMIUM_PATH`/`PUPPETEER_EXECUTABLE_PATH` all unset (so `resolveChromiumExecutable()`'s fixed-path fallback — which checks `/usr/bin/google-chrome` etc. directly, unaffected by `PATH` — could not silently supply a browser either; verified no browser is reachable this way and the build still succeeds only through the injected `engineBrowser`). Field guide: 34pp both legs, page size 621×810pt both legs, `Producer: Skia/PDF m148` (Electron) vs `m151` (CLI/system), file size within 5 bytes. Design guide (0 diagnostics either path): `inspectPdf()`'s `namedDests` — the exact page-map/cross-reference mechanism Tier 3 and the parity gate depend on — is **byte-for-byte identical** between the Electron-hosted build and the CLI's external-Chromium build: `{"folio-m-1":0,"folio-m-2":2,"ch-typography":6,...,"ch-cli":47,"cli-reference":47}` (21 entries), `linkTargets` count 13 on both. One real bug found and fixed while proving this: the first Electron-browser implementation memoized ONE `BrowserWindow` across every `newPage()` call, which broke `compiler/build.ts`'s §10 predict-then-verify pass (it opens a SECOND concurrent page while the first is still rendering) — the shared session's debugger got detached mid-flight by the throwaway page's cleanup, hanging the whole build forever with no error. Fixed by giving every `newPage()` call its own window (mirrors `cdp.ts`'s own per-target model); `close()` now tears down every window the `Browser` ever opened. Full verification: `packages/cli` `bun test` 2346 pass/1 skip/0 fail (unchanged from before this work), `packages/desktop` `npm test` 2424 pass/0 fail, `bun scripts/native-parity-gate.ts` with the (already-empty) `KNOWN_DIVERGENCES` allowlist — 5/5 fixtures, 0 divergences, gate PASSES — and a plain CLI native build of the field guide (external Chromium 151, unaffected by any of this) still produces the identical 34pp/621×810pt/37,978,532-byte PDF it always has. |
| 08-09 (WP-B REVIEW, independent) | **F.25 desktop export w/o system Chromium — RE-VERIFIED by an independent run; one real gap found and closed** | works (Electron 148, no external browser reachable at all) | works | **=** | Independent re-measurement of the row above, run from scratch rather than trusted. Masking was made STRICTER than the original proof: the desktop-shaped Electron main process ran inside a private mount namespace with (a) the real Chrome binary `/opt/google/chrome/google-chrome` bind-mounted to `/dev/null` (`/usr/bin/google-chrome --version` → `Permission denied`, exit 126 — so `resolveChromiumExecutable()`'s fixed-path fallback cannot supply one), (b) `~/.cache/puppeteer` replaced by an empty tmpfs (0 entries — so puppeteer's own download cache cannot either), (c) `PATH=/nonexistent` and every `CHROMIUM`/`PUPPETEER` env var unset. GAP FOUND AND CLOSED: an injected `engineBrowser` bypassed `connectChromium()`, which is where the `REQUIRED_MILESTONE` floor is actually enforced — `build-runner.ts` skips `verifyNativeChromiumMilestone()` on that path and nothing else checked it, so the engine's one hard guarantee was unenforced on exactly the host that supplies its own browser. `engine.ts`'s `buildNativePdf` now checks `browser.milestone` for the injected path (and closes the host's browser before throwing); covered by `src/lib/engine.injected-browser.test.ts`. Two stale user-facing strings that this work falsified were also corrected (`chromium.ts`'s and `build-preflight.ts`'s "--engine native always drives a separate, external Chromium" notes, plus a `REQUIRED_MILESTONE (151)` comment). | Measured by the reviewer, after the fix: `/tmp/fbtest/book` 2pp 432×288pt, field guide **34pp 621×810pt** (37,978,537 B, 17.6s), design guide **53pp 612×792pt** (507,521 B, 7.9s) — all three `Producer: Skia/PDF m148`, i.e. printed by Electron's own Chromium, while the CLI's external-Chromium builds of the same books report `m151` at 34pp/621×810pt and 53pp/612×792pt. Cross-engine identity: design-guide `namedDests` 21/21 and `linkTargets` 13/13 **identical** between the m148 Electron build and the m151 CLI build; page 17 rasterized at 60 dpi is **pixel-identical** (diff bbox `None`, 0 non-zero pixels of 336,600). `REQUIRED_MILESTONE === 148` confirmed in source. Spike suite re-run on BOTH binaries — Chromium 148 (`~/.cache/puppeteer/chrome/linux-148.0.7778.97`) and Chromium 151 (`/usr/bin/google-chrome-stable`): **12/15 on each, the same three failures on each** (s8, s9, s14 — pre-existing, unrelated to this work), so the loosened s0/s2 assertions pass in both regimes. Font flags re-measured independently: `--font-render-hinting=none` on vs off → PDFs differ in **4 bytes** (the `/CreationDate`+`/ModDate` timestamp) and the 150 dpi raster is pixel-identical (0 non-zero diff pixels), confirming the decision not to add it; `--disable-gpu` could NOT be re-measured — Electron hung past 90s and 110s in this container with that switch set, which is itself a reason not to add it. Suites: `packages/cli` `bun test` 2346 pass/1 skip/0 fail, `packages/desktop` `npm test` 2424 pass/0 fail, `native-parity-gate.ts` 5/5 fixtures 0 divergences with `KNOWN_DIVERGENCES` confirmed empty in source, `build-engine-bundles.mjs --force` regenerates byte-identical bundles. `--engine paged` build of `/tmp/fbtest/book` still renders (1pp, 432×288pt, 22,061 B — same size as the pre-change artifact) and was rasterized and eyeballed. |

### WORK PACKAGE B — engine improvements (divergence now allowed)

| Date | Area | Native | Paged | Verdict | Evidence |
|---|---|---|---|---|---|
| 08-09 (WP-B item 1) | **`Gutterpress.refresh()` soundness — FIXED** | rebuilds the strip structure (`unwrapStrips()` + `buildStrips()`) before re-measuring, so a spliced-in page-context change is visible | n/a (Paged.js has no equivalent incremental-refresh primitive) | fixed | `fragment.ts`'s `relayout()` used to only re-`measure()` the strips built at mount, never re-running `buildStrips()`/`explodeChildren()` — the exact unsound-refresh finding recorded above. New `unwrapStrips()` undoes `buildStrips()` (and any `decorate.ts` `.folio-run` decoration chrome wrapped around a strip since mount — the first cut left stale chrome behind as an orphan the next `buildStrips()` swept up as if it were authored content, measured as 2 ghost pages of decoration ahead of the real rebuilt strip; fixed by unwrapping through the run wrapper too) before calling `buildStrips()` again. `refresh.test.ts` (new, Chromium-driven): splices a `.on-named` div (assigned a distinct `@page named` via CSS already present at mount) into a 1-page fixture and calls `refresh()` — before the fix this silently stayed at 1 page; after the fix it reports 2, matching a fresh navigation of the equivalent final DOM (`refresh-splice-expected.html`) exactly. |
| 08-09 (WP-B item 2) | **Build options threaded through (title/author/signature) — FIXED** | `pdfinfo` reports `/Author` on a native PDF/X build (previously never set, B.12) | unaffected (out of scope — paged has no equivalent option-passing seam to fix) | fixed (native only; shared paged gap left as documented) | `lib/engine.ts`'s `buildNativePdf()` dropped `title`/`author`/`signature` even though `build()` already implements them (engine's `postprocess.ts`) and the manifest already resolves `title`/`authors`. Added `NativePdfOptions`, threaded `config.title`/`config.authors.join(", ")`/`config.print.signature` from `build-runner.ts`. Added the one missing manifest key for an implemented-but-unreachable feature: `print.signature` (`ResolvedConfig.print.signature`, default `1` = no padding, both presets). Verified: built the design guide `--engine native --format pdfx`, `pdfinfo` shows `Author: Your Name` (manifest's `authors: ["Your Name"]`) where before this session it was always absent. Signature itself remains untested end-to-end (no fixture asks for one) but is now reachable via `manifest.yaml`'s `print: {signature: N}` — same status change B.12 asked for ("recorded as untested, not as parity" → now wired, still untested by a fixture). |
| 08-09 (WP-B item 3) | **`--strip-annotations` — FIXED, actually strips now** | 0 `/Subtype /Link` objects survive (was 20/20 before-and-after, B.12) | 0 `/Subtype /Link` objects survive (same fix — shared function) | fixed (both legs, since `ghostscript.ts` is engine-agnostic) | Root cause: qpdf's `--flatten-annotations=all` draws annotations WITH an appearance stream into page content before removing them, but Link annotations have no appearance stream (that's what makes them invisible), so qpdf's flatten step had nothing to draw and left every one in place. `stripAnnotations()` now also deletes any remaining `/Annots` per page via pdf-lib (in-process, no new external tool) after the qpdf pass. Verified: built the design guide `--engine native --format pdfx --strip-annotations`, normalized with `qpdf --qdf --object-streams=disable`, `grep -c "/Subtype /Link"` → 0 (was 20). Re-ran `--engine paged` with the identical flag — also 0 (was 21) — confirming the fix is shared, not native-only. Content integrity checked: 53pp, fonts embedded (`pdffonts`), 48,345 chars extractable (`pdftotext`), rendered p1 and looked — unchanged. |
| 08-09 (WP-B item 4) | **PDF/X rasterization — root-caused and now detected + warned, NOT silently changed** | warns precisely when triggered (field guide); silent on the design guide (no alpha-channel art) | same (shared `ghostscript.ts`/`build-runner.ts` code path) | root-caused; conformance intentionally left unchanged | Root cause confirmed via `pdfimages -list` on the field guide's plain-PDF output: 14 image/smask pairs (e.g. `1275×851 icc` + its `1275×851 gray smask`) — Chromium emits a real PDF soft mask for every `<img>` whose source PNG carries an alpha channel (`/tmp/fg-proof-parent/field-guide/images/wf.png`: confirmed `8-bit RGBA`, alpha extrema `(0, 255)` — genuine partial transparency, not an inert fully-opaque channel that could be safely stripped without a visual change). PDF/X-1a:2001 and PDF/X-3:2002 are both based on PDF 1.3 by spec (`ghostscript.ts`'s fixed `-dCompatibilityLevel=1.3`, required for either flavor's conformance, not a Gutterpress default) — PDF 1.3 predates the PDF 1.4 transparency model entirely, so Ghostscript's only PDF/X-compliant move is flattening the whole page with a soft mask down to one raster image. Raising the compatibility level would silently break the very conformance the flag claims (ruled out per this work package's explicit instruction). Implemented option 3 (detect + warn precisely): `hasSoftMaskedImages()` (`ghostscript.ts`) does a literal `/SMask <n> <n> R` scan of the raw PDF (distinguishing a real mask reference from an opaque image's inert `/SMask /None`) right after the raw PDF renders and before Ghostscript runs; `build-runner.ts` logs the exact consequence and two concrete fixes (flatten the art's alpha before including it, or build plain `--format pdf` if PDF/X isn't needed). Verified: field guide build logs the warning; design guide build (no embedded raster art referencing alpha) reaches "Converting to CMYK PDF/X" with no such warning. Unit tests in `ghostscript.test.ts` cover the real-reference, `/SMask /None`, and no-image cases. |
| 08-09 (WP-B item 5) | **Preview TOC "p.?" — root-caused as a BOOK CONTENT bug, not an engine gap; preview now warns instead of staying silent** | warns per broken link (new); resolves correctly when the target exists (unchanged, already worked) | shows "p.0" for the same broken links (Paged.js's own unresolved-target convention; not touched — out of native engine scope) | root-caused; content bug, not fixed in the book (out of scope) — preview honesty fixed | Drove the field guide's native HTML preview with Puppeteer and read `window.Gutterpress.decoration.targets`/`document.getElementById()` directly: of 13 TOC links, only `#chapter-01` has a matching id anywhere in the rendered DOM (and it resolves correctly, "p.5") — the other 12 (`#chapter-02-0`, `#chapter-03`, …) point at ids that simply do not exist in the book (the actual heading ids are content-slugged from heading TEXT, e.g. `specialty-augmerc`, not derived from the TOC's assumed chapter-file-name scheme). This proves the viewer's `target-counter()` machinery (`pageOf`/`pageRangeOf`/`api.targets`) resolves correctly whenever a target genuinely exists — the "?" is not a resolution capability gap, it is 12 dead links in this book's TOC markdown, a content bug for the book to fix (out of this work package's scope per the architecture rule: book content bugs get fixed in the book, not the engine). What WAS in scope and fixed: the live preview gave the author zero explanation for the "?", where the compiler's PDF build already has an identical, actionable diagnostic (`engine.xref.broken` in `compiler/build.ts`) the preview had no equivalent of. `decorate.ts`'s `fillXrefs()` now pushes the same "doesn't point at anything in this book" message into `Gutterpress.decoration.warnings` for every unresolved `target-counter()` reference. Verified on the field guide: 12 warnings, one per dead TOC link, exact hrefs named. New fixture-driven regression test `xref-warning.test.ts` proves both halves: a broken href ("#nope") warns and still shows "p.?" (there is no real page to show), a valid href ("#real") resolves to "p.1" with no warning. |

### WORK PACKAGE B — independent review pass (08-09)

| Date | Area | Native | Paged | Verdict | Evidence |
|---|---|---|---|---|---|
| 08-09 (WP-B review) | **WP-B item 4 CORRECTED — the transparency detector missed CSS `opacity` entirely** | warns on every live-transparency form now | same (shared `ghostscript.ts` path — re-verified on the paged leg) | fixed (follow-up commit) | Independent review re-measured item 4 and found the shipped `hasSoftMaskedImages()` byte scan silently missing an entire trigger class. `/SMask <n> <n> R` works for alpha-channel IMAGES (image XObjects are streams, so their dicts sit uncompressed at top level) but CSS `opacity`/`mix-blend-mode` compile to a `/Group << /S /Transparency >>` and `/ExtGState` dicts, which Chromium packs into deflate-compressed **object streams** (`/Type /ObjStm`) where no regex can reach them. Measured with a probe book carrying one `opacity: 0.5` rule and ZERO images: PDF/X output fully rasterized (`pdffonts` empty, `pdftotext` empty) with **no warning at all** — precisely the "author discovers a fontless PDF after the fact" outcome item 4 was closed to prevent, and the likelier trigger of the two for a project whose authors style via CSS custom properties. Replaced with `hasLiveTransparency()`, which walks the pdf-lib-parsed object graph (object streams included) for image soft masks, graphics-state soft masks, `/ca`+`/CA` < 1, and transparency groups; returns false rather than throwing on unparseable bytes (an advisory warning must never fail a build). Re-verified end-to-end against four books with known Ghostscript outcomes — design-guide (no warn, 14 fonts kept), fbtest (no warn, 2 fonts kept, opaque RGB image), opacity probe (WARNS, 0 fonts — was silent), field-guide (WARNS, 0 fonts). Predicate is now exactly right on all four where the byte scan was wrong on one. Paged leg re-checked on the opacity probe: warns, 0 fonts — the fix is engine-agnostic as claimed. |
| 08-09 (WP-B review) | **WP-B item 5 — mechanism confirmed, but the warning has NO CONSUMER** | 12 warnings correctly recorded in `Gutterpress.decoration.warnings`, one per dead TOC link | n/a | confirmed as implemented; **author-visibility gap remains open** | Independently reproduced the root cause exactly: drove the field guide's built native viewer and enumerated every `a[href^="#"]` carrying a rendered page-number slot — 13 total, of which `#chapter-01` is LIVE and resolves to `" p.5"`, and the other 12 are DEAD (no matching id in the DOM) and render `" p.?"`. Confirms the "?" is 12 dead links in the book's own TOC markdown, NOT a `target-counter()` resolution gap. `decorate.ts`'s new warnings are genuinely populated (12 entries, exact hrefs). **However**: `grep` across `packages/cli/src` and `packages/desktop/src` finds no consumer of `decoration.warnings` anywhere — not the preview server, not the desktop UI. The array is reachable only by typing `window.Gutterpress.decoration.warnings` into a devtools console, so an author looking at the live preview still sees a bare "p.?" with no explanation. The item's mechanism is real and correctly implemented; the "the preview is honest about why" outcome is not yet delivered to the author. Surfacing it is a product decision (preview server log? desktop Problems panel?) deliberately left to the owner rather than invented here — note that `checkOverflow()`'s designer warnings share the same unconsumed array, so this is a pre-existing plumbing gap the item inherited, not one it introduced. |

### WORK PACKAGE C — native becomes the default (08-09)

| Date | Area | Native | Paged | Verdict | Evidence |
|---|---|---|---|---|---|
| 08-09 (WP-C) | **Engine resolution default flipped: `native` — DONE** | no `engine:` key resolves to native | `engine: "paged"` still fully supported, now logs a one-line deprecation notice (`warnOnce`, id `engine-paged-deprecated`) pointing at the migration note | done | `manifest.ts`'s cascade changed from `c.engine ?? m.engine ?? "paged"` to `c.engine ?? m.engine ?? "native"`. Swept every other place `paged` was documented/coded as the default: `schema/manifest.types.ts` + `assets/manifest.schema.json` (JSDoc/description + JSON `"default"` key), `types.ts`, `cli-args.ts`, `build-runner.ts`, `preview/file-watcher.ts` doc comments, `commands/build.ts`/`preview-args.ts` CLI help text, `packages/cli/README.md` (both `--engine` lines), `docs/schema-autocomplete.md`, and the desktop's `readProjectEngine` fallback (`web-adapter.ts`) + `electron/preview/controller.ts`'s no-manifest fallback var. Wrote the author-facing migration note (`docs/migrations/2026-08-native-engine-default.md`), covering the default flip, how to opt back into `paged`, and the known author-visible differences already recorded in `docs/native-only-migration-plan.md` (margin boxes can't `transform`/`box-shadow` or read `counter(page)`; `counter-reset: page` is synthesized, not CSS-driven; the standalone-image `fit-content` sizing default; the named-page forced-break behavior; the external-Chromium-milestone requirement for native PDF export). |
| 08-09 (WP-C) | **Verification: no-engine-key build is measurably native** | `/tmp/fbtest/book` (no `engine:` key) built with no `--engine` flag: log line "Rendering HTML to PDF via the Gutterpress engine (native Chromium pagination)", 2pp | same book with `--engine paged`: log line "Rendering HTML to PDF via Chromium+Paged.js", 1pp, deprecation notice logged exactly once | pagination difference confirms the default really is native (matches the already-documented full-bleed named-page divergence for this fixture, not a new one) | `bun src/cli.ts build /tmp/fbtest/book --out /tmp/wpc-verify/default.pdf` (no `--engine`) vs `bun src/cli.ts build /tmp/fbtest/book --engine paged --out /tmp/wpc-verify/paged.pdf`: `pdfinfo` reports 2 pages (native) vs 1 page (paged) — the same `page:`-transition forced-break divergence already classified as a Paged.js bug in the WP-C row above ("`/tmp/fbtest/book` full-bleed image placement — CLASSIFIED"). Also confirmed programmatically: `resolveConfig({}, manifest).engine === "native"` for a manifest with no `engine:` key. (CORRECTED in the 08-09 review pass: this row originally cited "the field guide's committed manifest (no `engine:` key)" — the field guide's manifest DOES pin `engine: native` explicitly, so it never exercised the default. `/tmp/fbtest/book` is the fixture with no `engine:` key.) Ran the field guide (34pp native / 32pp paged — the pre-existing delta recorded in the C.13 review row) with `--engine paged` and grepped the log for "deprecated": count is exactly 1, not once per page/chapter. `native-parity-gate.ts` re-run with the new default, empty allowlist: 5/5 fixtures, 0 divergences. Desktop: `electron/preview/controller.ts`'s no-manifest engine fallback flipped to `"native"` to match; full desktop suite (2424 tests) and CLI suite (2344 tests, 1 skip) both green after fixing the tests that had relied on the old implicit-paged default (`build-runner.browser-lifecycle.test.ts`, `file-watcher.test.ts`, `http-server.test.ts`, `manifest.test.ts`'s `engineStyles` suite — all now pass `engine: "paged"` explicitly where that was the behavior under test, rather than relying on omission). |
| 08-09 (WP-C review) | **Independent re-verification of the default flip — CONFIRMED, by artifact not by log line** | `/tmp/fbtest/book` (no `engine:` key), no `--engine`: 2pp; page 2 rasterized at 80dpi shows the `.full-bleed` plate alone on its own sheet, painted edge-to-edge horizontally — i.e. `@page gp-full-bleed { margin-left/right: 0 }` applied, which is the named-page behavior ONLY the native leg produces | same book `--engine paged`: 1pp; the plate sits inline under the heading and is INSET by the 0.75in side margins — the named page's margin override never applied | `>` native (spec-correct named-page break + margin override); the artifact difference, not the log string, is what proves which engine ran | Rasterized both legs (`pdftoppm -png -r 80`) and LOOKED at all three pages. `pdftotext` gives identical text on both legs ("Full bleed check" / "MEASURE-GLYPH-BASELINE-TEXT") — no content lost in the reflow. Deprecation notice: `grep -c deprecated` = **1** on the paged fbtest build and **1** on the paged field-guide build (32pp), **0** on every native build. Suites re-run from scratch: CLI `bun test` 2344 pass / 1 skip / 0 fail; desktop `npm test` 2424 pass / 0 fail. `bun scripts/native-parity-gate.ts` re-run, `KNOWN_DIVERGENCES` verified empty in source: 5/5 fixtures (fbtest 2pp, field-guide 34pp, book-01 2pp, book-02 2pp, design-guide 53pp), 0 divergences, "gate PASSES". No files under `src/engine/` changed in either WP-C commit, so no bundle regeneration was due. TWO DOC DEFECTS FOUND AND FIXED in this pass (see the follow-up commit): (1) the migration note claimed `--format html` was "unaffected by this default flip" — MEASURED FALSE: the exported `book.html` for a no-`engine:`-key book drops the Paged.js polyfill `<script>` and its runtime CSS (329 → 4 `paged` occurrences); the note now documents this as a change, with the escape hatch. (2) this table's WP-C verification row cited the field guide's manifest as having no `engine:` key — it pins `engine: native`; corrected in place. RESIDUAL, NOT FIXED (recorded, low risk): `assembleBookHtml`'s own `engine?` option still means "paged" when omitted (`assemble.ts:186`), and the desktop SPA's `ProjectLifecycleController.previewEngine` still initializes/resets to `"paged"`. Both are unreachable today — every real call site (`build-runner.ts`, `preview/file-watcher.ts`) passes the resolved `config.engine`, the PWA path pins `"paged"` deliberately (WP-C item 2), and `previewEngine` is overwritten by `PreviewStartSuccess.engine` before any consumer (all gated on a live preview `client`) reads it — but they are the two places where a future omission would silently mean Paged.js. |

### Scope caveat on the parity gate

`native-parity-gate.ts` compares **native-print vs native-viewer** — it proves the preview does not lie about the PDF. It does **not** compare native vs Paged.js; that is what this table is for.

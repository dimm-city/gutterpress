# SFE-P0a — Execution baseline verification and record

## Objective

Record the exact post-`0.10.2` execution baseline and reproducible current behavior
(mutation callers, platform/transport surface) before any feature or deletion work,
so every later deletion can prove its inventoried callers moved or disappeared.

## Allowed behavior changes

- None. This run adds documentation and characterization tests only.

## Behavior that must remain unchanged

- All production code, configuration, dependencies, and public contracts.
- Existing tests: none may be modified, weakened, or deleted.

## Binding decisions

- **D1** — Baseline: `0.10.2` merged to `main`; work branch created from the recorded
  `origin/main` commit. Vocabulary: source/snapshot/source edit/projection/generated
  view/host/preview have exactly one meaning.
- **D15** — Every deletion claim requires search proof, dependency proof, and passing
  behavior tests. Every run records exact commands, exit codes, test counts, base SHA,
  head SHA.
- **Lane rules** — write only assigned paths; no ProseMirror-family dependencies; no
  new desktop HTTP routes.

## Recorded baseline facts (verified by the integrator before this run)

- Baseline `origin/main` SHA: `ea7b60d50340b75b9c58666e5063bcbbbb666576`
  (merge of PR #196, `release/0.10.2`).
- Work branch: `claude/sonnet-opus-agent-workflow-4s81ps` — **deviation**: the plan
  names `feature/source-first-rich-editor-architecture`; the execution harness
  designates this branch instead. Ancestry holds: the work branch was created at
  exactly the baseline SHA.
- **Deviation (release management, non-blocking):** the final `v0.10.2` tag has not
  been pushed (latest tag `v0.10.2-beta.2`; package versions read `0.10.2-beta.2`),
  and `origin/release/0.11.0` does not exist yet. The plan's equality precondition
  is vacuously safe — there is no release branch to diverge from and the work branch
  equals `origin/main` — so execution proceeds; release management must create
  `release/0.11.0` from this baseline and cut the final `0.10.2` tag.

## Behavior table

| Case | Baseline | Required result | Diagnostic/state | Test owner |
|---|---|---|---|---|
| Baseline record | No `baseline.md` | `baseline.md` with SHA, ancestry proof, counts, exact commands | doc | A |
| Deletion-ledger counts | Placeholder table | Measured baseline counts filled in | doc | A |
| Preview mutation inventory | Undocumented | Every preview mutation caller and protocol message listed with file:line | doc | B |
| CommitEngine gates | Tested in `commit-engine.test.ts` | Coverage inventory; missing pins added (characterization only) | bun:test | B |
| InlineEditController lifecycle | Untested or partially tested | Characterization pins for begin/end/generation/stale paths that P4 deletion relies on | bun:test | B |
| Platform surface | Undocumented | `Platform`/`HostServices` methods grouped by context, consumers classified feature/dead/test-only | doc | C |
| Transport surface | Undocumented | All `+server.ts` routes, `api.ts` methods, preload methods, IPC handlers inventoried and cross-mapped | doc | C |

## Lane ownership

| Lane | May write | Must not write | Deliverable |
|---|---|---|---|
| A | `docs/plans/source-first-editor/baseline.md`, `docs/plans/source-first-editor/deletion-ledger.md` (baseline table only) | tests, production code | Baseline record + measured counts |
| B | `packages/desktop/tests/editor/**` (new files only), `docs/plans/source-first-editor/mutation-inventory.md` | existing test files, production code, Lane A/C docs | Mutation characterization + inventory |
| C | `docs/plans/source-first-editor/platform-inventory.md`, `packages/desktop/tests/platform/**` (new files only) | existing test files, production code, Lane A/B docs | Platform/transport inventory |

## Caller and consumer inventory

- This run PRODUCES the inventories; it consumes nothing.

## Persistence and compatibility

- No user data, no migrations, no public contract changes.

## Security and trust

- Documents may not contain secrets, tokens, or absolute user paths.

## Determinism and resource limits

- All counts generated by commands recorded verbatim in the docs, reproducible at the
  baseline SHA.

## Test plan

- Characterization tests only (bun:test, `packages/desktop/tests` conventions):
  pinned assertions that trip on unintended later change.
- New tests must pass at the baseline; liveness assertions required (no vacuous pins).

## Review dimensions

- Is every count generated from the recorded baseline with an exact command?
- Are feature callers distinguished from dead or test-only callers?
- Can later deletion prove every inventoried caller moved or disappeared?
- Do the new characterization tests pin behavior tightly enough that the P4/P5
  deletions would trip them if semantics silently changed beforehand?

## Gate

> Never use `bun --cwd <pkg> run <script>` for any gate command below. On
> Bun 1.3.x that form prints the `bun run` usage banner and EXITS 0 without
> running the script at all — a gate reading only the exit code would report
> green having run nothing (verified on this tree with bun 1.3.11: both
> `bun --cwd packages/open-design-plugin run test` and
> `bun --cwd packages/desktop run test` exit 0 and print the usage banner,
> not test output — the exact defect `.github/workflows/ci.yml` already
> carries a NOTE about). Use `cd packages/<pkg> && bun run <script>` instead.
> This run's own recorded evidence (mutation-inventory.md §6) already used
> the robust form (`bun run test` executed from inside each package), so no
> past evidence here is invalidated — only this section's literal command
> text was wrong. A gate command that produces no test or typecheck output
> is a failed gate, not a pass — record the actual test/type-check counts,
> not just the exit code.

- `bun run typecheck`
- `cd packages/desktop && bun run test`
- `cd packages/cli && bun run test`

## Review log

- **Round 1** (adversarial review of `764613ec..5a53aa4e`, two batches: plan/inventory
  docs and characterization tests): 5 CONFIRMED findings — a false coverage claim in
  mutation-inventory §4.2, missing context-menu identifiers in the mutation inventory,
  a dropped `lint` namespace plus double-counted reconciliation in platform-inventory
  §11, a misleading test title, and gate commands using the silently-no-op
  `bun --cwd <pkg> run <script>` form. All fixed in `17929629`, each fix with a
  sabotage/reproduction proof.
- **Round 2** (re-review): 1 repair-introduced CONFIRMED finding — §1.5 wrongly
  claimed a production caller for `PreviewClient.getContextTargetAt()`; reclassified
  TEST-ONLY with grep proof. Fixed in `63d9d5da`. Verdict: **approve**, 0 confirmed
  remaining, 1 advisory (citation line-range precision).
- **Gate**: PASS — typecheck exit 0; desktop 2132 pass/0 fail; cli 1810 pass/0 fail;
  production paths byte-identical to baseline `ea7b60d5`.

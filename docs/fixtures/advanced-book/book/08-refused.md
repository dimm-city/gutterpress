@chapter "The Refused Copy" #ch-refused

@page

# The Refused Copy

One drill below is written against regulation: prose separates the
count marker from its count, so the desk cannot tie the two together.
The review pipeline must send this file back rather than guess at what
the writer meant.

@track "Orphaned count"

This aside sits between the marker and its count, which is exactly the
separation the regulation forbids.

1. A count the marker above cannot claim.
@end-track

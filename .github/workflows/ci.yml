name: CI

on:
  push:
    branches: [main, develop, "release/**"]
  pull_request:
    branches: [main, develop, "release/**"]

env:
  # Skip Puppeteer download during dependency installation
  # We'll use system Chrome instead to avoid 403 errors
  PUPPETEER_SKIP_DOWNLOAD: 'true'
  PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: 'true'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable

      # The PDF/X tests self-skip when their external tool is absent
      # (ghostscript.integration.test.ts uses `ghostscript ? test : test.skip`),
      # so without these the suite reports green while never exercising real
      # PDF/X color conversion at all.
      - name: Install PDF tooling (Ghostscript, qpdf)
        run: |
          sudo apt-get update
          sudo apt-get install -y ghostscript qpdf

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies
        run: bun install

      - name: Configure Puppeteer to use system Chrome
        run: |
          echo "PUPPETEER_EXECUTABLE_PATH=$(which google-chrome-stable)" >> $GITHUB_ENV

      - name: Run tests
        run: bun --filter gutterpress test

      # Galley codec soundness over every example book: zero lost words in
      # both serialization modes, canonical idempotence, opaque rate <= 2%,
      # byte-identical untouched files >= 80% (Galley v2 —
      # docs/tiptap-galley-architecture.md).
      - name: Galley round-trip corpus gate
        working-directory: packages/cli
        run: bun run roundtrip:gate

      # Preview/print parity: the on-screen viewer derives pagination by a
      # different route than the print fragmenter, so this proves the two
      # still agree. CLAUDE.md calls it permanent and required-green with an
      # empty allowlist — but it was never wired into CI, so nothing enforced
      # that, and its first real run (2026-08-16) FAILED.
      #
      # What it reports: 14 divergences, ALL confined to the one fixture
      # docs/fixtures/css-authoring-spike/book. Every other fixture is clean
      # (with-design-guide book-01 2pp, book-02 2pp, design-guide 54pp,
      # gp-image-positioning 7pp — 0 divergences each). On that fixture print
      # measures 7pp where the viewer reports 6pp; instrumented headings from
      # gp-parity-h2 onward map one page earlier in the viewer (ch-gallery two
      # pages earlier), and target-counter() values skew to match.
      #
      # Root cause is NOT yet known, and in particular it is NOT the defect
      # this fixture was originally committed to pin. That one was a
      # `position:absolute` <gp-anchor> as the first box after a forced break
      # measuring one page late in print; it was fixed by making the anchor
      # `display:inline` (compiler/agent.ts `ensureAnchor`) and is pinned by
      # build.running-heads.test.ts. Reproducing THIS divergence needs
      # Chromium >= 148 (the engine's own milestone gate), so it cannot be
      # bisected in a dev container running an older build.
      #
      # It is NOT caused by the galley branch: that branch has an empty diff
      # against this script, the fixture, src/engine/compiler/, and
      # viewer/fragment.ts, and BOTH sides of the comparison call
      # `fragmentDocument()` directly (build.ts:1130, this script:245) — never
      # `mount()`, so the branch's `layoutBracket` cannot reach either path.
      #
      # Reported, not blocking, so an unrelated engine bug does not gate every
      # PR — but it now runs on every push instead of never, so it cannot be
      # forgotten again. Flip continue-on-error off the moment the divergence
      # is fixed; that restores the invariant CLAUDE.md already claims.
      - name: Preview/print parity gate (reporting — known divergence)
        working-directory: packages/cli
        continue-on-error: true
        run: bun run parity:gate

      - name: Test Open Design plugin contract
        working-directory: packages/open-design-plugin
        run: bun test

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Check release distribution tooling and metadata
        run: |
          node tools/prepare-release-assets.test.mjs
          node tools/update-package-managers.test.mjs
          node tools/update-package-managers.mjs --check

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies
        run: bun install

      - name: Build CLI npm bundle
        working-directory: packages/cli
        run: bun run build

      # NOTE: never use `bun --cwd <pkg> run <script>` in workflows — on Bun
      # 1.3.x that form prints `bun run` usage and EXITS 0 without running the
      # script, so the step passes while doing nothing (review finding: this
      # made the desktop-build and type-check steps silent no-ops).
      # `working-directory` + `bun run <script>` is the robust form.
      - name: Build desktop (static SPA + Electron main)
        working-directory: packages/desktop
        run: |
          bun run build
          bun run electron:build

      # Self-test for the guardrail script itself (dependency-free by design):
      # if a refactor breaks its detection regexes, this must fail loudly
      # rather than letting the real gate below pass vacuously.
      - name: Check renderer purity self-test
        run: node tools/check-render-purity.test.mjs

      # §8 renderer-purity guardrail: fail if host/node code leaked into the
      # built desktop CLIENT SPA bundle. Scope is build/client/ ONLY —
      # adapter-node's build/server/ + build/handler.js are host Node code by
      # design (§8). --strict: the build step above just ran, so an absent
      # build/client means the build silently did nothing — that must FAIL,
      # not skip (review finding: the non-strict form passed vacuously while
      # the broken `bun --cwd` build step produced no output at all).
      - name: Check renderer purity (CLAUDE.md §8)
        run: node tools/check-render-purity.mjs packages/desktop/build/client --strict

  desktop-test:
    name: Desktop Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies
        run: bun install

      - name: Configure Puppeteer to use system Chrome
        run: echo "PUPPETEER_EXECUTABLE_PATH=$(which google-chrome-stable)" >> $GITHUB_ENV

      # The desktop suite imports gutterpress by name (resolves to its
      # built dist/), so build the package before running desktop tests — same
      # prerequisite and command as release.yml's `test` job, kept identical
      # so the two gates cannot drift apart.
      - name: Build package dist
        run: bun run build
        working-directory: packages/cli

      # Keep this explicit even though the full desktop suite also runs it. If
      # the suite wiring changes, CI must still protect active-frame identity,
      # page-count-changing splices, and standalone Paged.js page metadata.
      - name: Run instant-preview splice contract
        run: node tests/preview-shell-regression.test.mjs
        working-directory: packages/desktop

      - name: Run desktop tests
        run: bun run test
        working-directory: packages/desktop

      - name: Run desktop lint
        run: bun run lint
        working-directory: packages/desktop

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies
        run: bun install

      # NOTE: `working-directory` + `bun run <script>`, never
      # `bun --cwd <pkg> run <script>` — on Bun 1.3.x the latter prints usage
      # and exits 0 WITHOUT running the script. Review finding: every step in
      # this job used that broken form, so the whole Type Check job was a
      # silent no-op that always passed.
      #
      # svelte-check and the electron tsc resolve gutterpress's types
      # to packages/cli/dist/*.d.ts (gitignored), so build the CLI dist first —
      # same prerequisite the desktop-test job already documents.
      - name: Build package dist (types for the desktop checks)
        working-directory: packages/cli
        run: bun run build:library

      - name: Run TypeScript compiler (CLI)
        working-directory: packages/cli
        run: bun run typecheck

      # The engine's browser-side sources (viewer, compiler agent, and the
      # inline-edit module) live in their own DOM-lib program — excluded from
      # the Node program above, so this is the ONLY thing that type-checks
      # them. Same class of gap as the Electron one below: the script existed
      # but nothing ran it.
      - name: Run TypeScript compiler (engine browser sources)
        working-directory: packages/cli
        run: bun run typecheck:engine-browser

      - name: Run TypeScript compiler (desktop SPA)
        working-directory: packages/desktop
        run: bun run check

      # audit F1: `check` above is svelte-check (SPA only). The Electron
      # main/preload TS was never type-checked in CI — its `typecheck` script
      # existed but nothing ran it, so a type error in electron/main.ts merged
      # green. Run it too.
      - name: Run TypeScript compiler (desktop Electron main/preload)
        working-directory: packages/desktop
        run: bun run typecheck

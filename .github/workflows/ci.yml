name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  # Skip Puppeteer download during dependency installation
  # We'll use system Chrome instead to avoid 403 errors
  PUPPETEER_SKIP_DOWNLOAD: 'true'
  PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: 'true'

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies
        run: bun install

      - name: Configure Puppeteer to use system Chrome
        run: |
          echo "PUPPETEER_EXECUTABLE_PATH=$(which google-chrome-stable)" >> $GITHUB_ENV

      - name: Run tests
        run: bun --filter @dimm-city/print-md test

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies
        run: bun install

      - name: Build CLI npm bundle
        working-directory: packages/cli
        run: bun run build

      - name: Build viewer (static SPA + Electron main)
        run: |
          bun --cwd packages/viewer run build
          bun --cwd packages/viewer run electron:build

      # §8 renderer-purity guardrail: fail if host/node code leaked into the
      # built viewer CLIENT SPA bundle. Scope is build/client/ ONLY —
      # adapter-node's build/server/ + build/handler.js are host Node code by
      # design (§8). Self-test: node tools/check-render-purity.test.mjs
      - name: Check renderer purity (CLAUDE.md §8)
        run: node tools/check-render-purity.mjs packages/viewer/build/client

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies
        run: bun install

      - name: Run TypeScript compiler (CLI)
        run: bun --cwd packages/cli run typecheck
        continue-on-error: true

      - name: Run TypeScript compiler (viewer)
        run: bun --cwd packages/viewer run check

  security-audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun (cached)
        uses: ./.github/actions/bun

      - name: Install dependencies with frozen lockfile
        run: bun install --frozen-lockfile

      - name: Run security audit
        run: bun audit || true
        continue-on-error: true

      - name: Check for vulnerabilities
        run: |
          echo "Running dependency security audit..."
          bun audit --json > audit-results.json || true
          if [ -f audit-results.json ]; then
            echo "Audit completed. Review audit-results.json for details."
          fi

@chapter #ch-toc .toc

# Table of Contents

<div class="lede">Navigate this guide by chapter. Each heading links directly to its chapter opener — click or tap in the PDF preview to jump.</div>

<ol>
<li><a href="#ch-typography">Typography</a> — Type scale, headings, body text, inline elements, token reference</li>
<li><a href="#ch-palette">Color Palette</a> — Brand tokens, swatches, usage rules, print considerations</li>
<li><a href="#ch-components">Components</a> — Callouts, sidebars, pull quotes, spec blocks, steps, glossary</li>
<li><a href="#ch-templates">Page Templates</a> — Cover, chapter opener, full-bleed, running headers, named pages</li>
<li><a href="#ch-layout">Layout</a> — Multi-column, image floats, page-break utilities, lede paragraph</li>
<li><a href="#ch-reference">Markdown Reference</a> — Every syntax feature with live examples</li>
<li><a href="#ch-cli">CLI Reference</a> — Build, preview, and publish commands</li>
</ol>

---

## How to use this guide

This guide renders with the **same stylesheet and page geometry** as your book. Every specimen is live — the type, spacing, and colors you see here are exactly what your book ships.

To make it yours:

1. Edit **§ 1 BRAND TOKENS** in `styles/guide.css` — colors, fonts, type scale.
2. Run `gutterpress preview design-guide` to see changes live.
3. Add book-specific rules to **§ 8 YOUR BOOK LAYER** at the bottom of `guide.css`.
4. Remove chapters you don't need; add chapters for components your book uses.

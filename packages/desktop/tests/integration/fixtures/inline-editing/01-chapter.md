# Inline Editing Smoke Chapter

This is the target paragraph for right click testing in the inline editing smoke test. It exists purely so a coordinate based click has a large stable rectangle to land on.

Second paragraph used as the keyboard Shift+F10 target. It sits far enough from the first paragraph that the two are unambiguous targets on the rendered page.

Third paragraph is here only to pad out the page so the running header and page number margin boxes are guaranteed to render above visible body content.

Filler paragraph number one, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number two, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number three, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number four, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number five, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number six, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number seven, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number eight, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number nine, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number ten, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

This deep paragraph is the click to source target. It lives far enough into the file, with plenty of filler both above and below it, that revealing it in the editor genuinely requires scrolling past the top of the document AND leaves enough trailing content for a true flush-to-top scroll position, which is the behavior this test actually needs to exercise.

Filler paragraph number eleven, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number twelve, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number thirteen, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number fourteen, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number fifteen, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number sixteen, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number seventeen, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number eighteen, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number nineteen, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

Filler paragraph number twenty, added only to lengthen the chapter's markdown source so that the CodeMirror editor pane cannot show the whole file without scrolling.

# Smoke Test

A minimal document to verify the gutterpress binary can build HTML output and
that the native engine renders it correctly.

## Section Two

Additional content to ensure the render pipeline runs to completion and
produces at least one `.folio-sheet` element.

/**
 * `@dimm-city/gutterpress-editor`'s framework-free web mount shell (SFE-P1a
 * Lane B). Re-exported from the package root (`../index.ts`) once the
 * integrator wires it in; nothing outside this package imports this barrel
 * directly yet.
 */
export * from "./mount.ts";
export * from "./diff.ts";

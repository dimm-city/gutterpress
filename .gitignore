# dependencies (bun install)
node_modules
.notes

# output
out
dist
*.tgz

# temporary test files
.tmp/

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
.claude/
dist/

# Code index and reference files (can be regenerated)
.references/
examples/field-guide/
tests/
# Integration fixtures and scripts are committed
!tests/integration/
!tests/integration/**
# Updater unit tests are committed (run in CI via the viewer `test` script)
!tests/updater/
# NOTE: the blanket `tests/` above excludes the tests DIRECTORY, so root-level
# committed harnesses (pagedjs-interface.test.mjs, preview-bridge.test.mjs) are
# tracked via `git add -f`. git's "can't re-include under an excluded parent"
# rule means a `!tests/*.test.mjs` negation can't bring them back.
!tests/updater/**
.reviews/
*.zip
*.skill
.trash/
.akm-run/
review/
review_screenshots/
_site/dc-design-guide-migration-check/
.print-md-stage/
web-ui-signing.key
